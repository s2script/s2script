# s2script — standing conventions & guardrails

- **The core owns every engine touchpoint.** Plugins never get raw detours; they get named, typed, multiplexed events + the single `HookResult` contract. Only exception: the explicit `unsafe` module.
- **Core is engine-generic; games are packages. Dependencies point one way: game → core, never core → game.** The core knows Source 2, never a specific game. Game classes, gamedata, descriptor bindings, team/weapon APIs live in `@s2script/cs2` (and future `@s2script/<game>`). A CI check fails the build if core/std imports a game package. Litmus test for any code: *would it still be true on a different Source 2 game?* If no → it's a game package, not core.
- **Never expose a raw pointer or raw cross-plugin reference across time.** Entities, shorter-than-plugin resources, and inter-plugin interfaces are handle/proxy-backed and host-invalidated; safe accessors return `T | null`; raw-live views are block-scoped and cannot cross `await`. Entity refs on the inter-plugin wire use the same `EntityRef`/`T | null` type as the entity system.
- **Cross-plugin comms are typed, versioned interfaces.** Methods = natives, events = forwards, one object, semver-governed. Hard deps return a proxy that throws on producer-unload; optional deps return `Interface | null`. All imports ledgered; unload resolves in reverse-dependency order.
- **`package.json` is the authoring format; reuse npm standards.** Standard fields for what npm models; the `s2script` block for engine facts (`apiVersion`, `publishes`, `pluginDependencies`/`optionalPluginDependencies`, `requiresGamedata`, `permissions`, `config`). `dependencies` = npm build-deps only; inter-plugin deps under `s2script`. Never overload npm's `exports`. The runtime consumes a derived minimal manifest baked into the `.s2sp`, never the full `package.json`.
- **npm scope taxonomy, one reserved official scope.** `@s2script/*` = first-party (engine-generic per-capability module packages `@s2script/entity`/`frame`/`timers`/`console`/`interfaces`; per-game `@s2script/cs2`/`@s2script/<game>` which ship that game's schema types; base plugins). `@<community>/*` = verified third-party; unscoped allowed. Never name a package `@s2script/core` ("core" is the native layer). Reserve `@s2script` everywhere from day one.
- **Layout is data, semantics are code.** Offsets/signatures/struct positions/interface strings live in regenerable `gamedata`/schema files; behavioral facts and name mappings in reviewed code. A field-offset change must never require a code change.
- **hl2sdk is a pinned, vendored, patch-capable dependency** and part of the update-day treadmill — it lags Valve, so own your schema/offset layer rather than trusting the SDK's game-class fields.
- **Contracts are versioned: engine `.d.ts`, host `apiVersion`, plugin semver.** Breaking any is a major bump that fails fast at the typecheck gate and again at load — never a silent runtime drift.
- **Degrade per-descriptor, never crash globally.** A broken signature/offset/field disables *that* descriptor with a named reason; the framework keeps running.
- **The ledger is the teardown authority.** Every persistent resource (including imported interfaces and exported-interface consumers) is auto-ledgered; teardown walks the ledger and doesn't depend on the plugin's own cleanup code running correctly.
- **Typecheck-gate every load and reload** against the shipped `.d.ts` *and* declared dependency interfaces. A failing file-watch reload leaves the running version untouched.
- **Lock the package contract early, build the registry late.** The `package.json`/manifest contract is designed in now so `s2script.com` is a distribution layer over an existing model, not a retrofit.
- **The base-plugin suite is the std lib's acceptance test.** The std lib isn't done until the `@s2script/base*` SourceMod-parity plugins build cleanly on it; awkwardness there is a std lib bug. They're CS2 plugins (depend on `@s2script/cs2`), registry-distributed std-lib consumers, not built into the runtime.
- **Build by risk, not by layer.** Thin vertical slices to a working end-to-end thread before breadth. Resist building breadth on an unproven spine.
- **The maintenance treadmill is a first-class feature.** Per-update gamedata/schema/hl2sdk regeneration + validation tooling is the moat. Design for green-within-48h-of-every-patch.

## Current state

**Slices 0–5B.2 complete and merged** (each proven on a live CS2 server). 0 — boot handshake (V8 in CS2 via Metamod). 1 — the `OnGameFrame` multiplexer (priority ladder, `HookResult` collapse, re-entrancy-safe snapshot, lazy detour). 2 — tick-integrated async (explicit microtask policy, `delay`/`nextTick`/`nextFrame`, a threadpool `threadSleep` marshalled back on the frame drain). 3 — a schema-backed typed accessor (`Pawn.health`, offset resolved live from SchemaSystem, `NetworkStateChanged` folded into the setter) living in `@s2script/cs2`. 4 — the milestone: one `.s2sp` that loads/hot-reloads/tears-down via **context-per-plugin + the ledger + the async-liveness guard**, built by `@s2script/cli` (`npx s2script build`). 4.5 — two plugins talk: a producer `publishInterface(name, version, impl)` exposes methods-as-natives + a `PublishHandle.emit` for forwarded events; a consumer `require("@demo/greeter")` gets a producer-backed **proxy** for a hard dep (throws `InterfaceUnavailable` while the producer is unloaded) or `Interface | null` for an optional one. Cross-context calls/events structured-copy their args; imports are auto-ledgered and teardown resolves in reverse-dependency order. 5A — **safe entities**: `Pawn` now holds an **`EntityRef` = `{index, serial}`** (no raw pointer crosses to JS) and **every field access re-validates the captured serial against the engine's live `CEntityIdentity`** — a stashed `Pawn` whose entity is destroyed reads `null` (serial mismatch), not garbage, not a crash. This **closes the Slice-3 raw-`ent` use-after-free**; the serial-gated natives (`__s2_ent_ref_valid`/`read_i32`/`write_i32`/`state_changed`, `__s2_handle_decode`) are engine-generic in core, CS2 names stay in `pawn.js`. Proven live: `bot_kick` → `stashed.health=null`, server keeps ticking; respawn → a fresh `forSlot` recovers with the new serial. **5A fast-follow — entity refs on the inter-plugin wire (deferral CLOSED):** an `EntityRef` now round-trips across the typed inter-plugin boundary *as a live ref* — a replacer tags it crossing the structured-copy wire, the consumer's context revives it bound to **its own** `__s2_ent_ref_*` natives; the received ref validates against the **shared** entity system and **flips to invalid across the plugin boundary** when the pawn is destroyed (cross-plugin host-invalidation). Proven live via `examples/entref-producer` + `examples/entref-consumer`: a producer-passed pawn ref reads `[consumer] received-ref valid=true health=100`, then `valid=false health=null` on `bot_kick`, server ticking throughout. **5B.1 — the schema catalog dump (treadmill):** the pure `Catalog` builder (`core/src/schema_catalog.rs`, engine-generic) + the shim's `schema_enumerate` SDK walk + the dev/treadmill native `__s2_schema_dump(path)` produce a regenerable **`games/cs2/gamedata/schema-catalog.json`** (committed) — every class → `{parent, fields}`, each field → `{name, offset, type:{kind, name?/inner?}}`; stored per-class own-fields with inheritance as the `parent` chain. Regenerated after each CS2 update (offsets move per patch). Proven live via `examples/schema-dump` on de_inferno: `[schema-dump] dump OK` → **2429 classes**; `CCSPlayerPawn`→`CCSPlayerPawnBase`; `m_iHealth`=`{atomic,int32}`@1456 (on `CBaseEntity`, matches the Slice-3 resolve). **5B.2 — typed field access:** `EntityRef` gains kind-dispatched typed read/write methods (`readFloat32`/`readBool`/`readInt8`/`readInt16`/`readUInt8`/`readUInt16`/`readUInt32`/`readHandle`→`EntityRef|null` + `writeFloat32`/`writeBool`) over a single generic `__s2_ent_ref_read`/`__s2_ent_ref_write` native pair — `__s2_ent_ref_read_i32`/`__s2_ent_ref_write_i32` retired into the generic path. `readHandle` decodes a `CEntityHandle` field and returns a live, serial-gated `EntityRef`. All reads are `T|null`, serial-gated. The demo plugin (`examples/demo-plugin`) exercises float32 (`m_flFriction`), bool (`m_bClientSideRagdoll`), and handle (`m_hOwnerEntity`) reads via `fresh.ref`. CS2 field names stay exclusively in the demo plugin (never in `core/src`). See the per-slice specs/plans under `docs/superpowers/`.

**Naming convention (locked in Slice 4):** PascalCase events + types (`OnGameFrame`, `Pawn`), camelCase functions + properties (`delay`, `nextTick`, `pawn.health`). **Inter-plugin marshalling (locked in Slice 4.5):** methods = natives, events (`on`/`off`/producer `emit`) = forwards, exposed as one proxy object; call args + event payloads cross the context boundary by **structured copy** (never a live pointer/reference); a hard dep is a proxy that throws `InterfaceUnavailable` on producer-absence, an optional dep resolves to `Interface | null`; every import is ledgered and unload walks reverse-dependency order.

**Entity safety (locked in Slice 5A):** entity refs are `EntityRef` = `{index, serial}` — a raw pointer never crosses to JS; every access resolves the pointer **and** compares the slot's live serial to the captured serial in a single lookup (no TOCTOU), degrading to `null`/`false` on mismatch; `Pawn` is `EntityRef`-backed and `Pawn.forSlot` decodes the controller's `m_hPlayerPawn` `CEntityHandle` via `__s2_handle_decode`. The game-package prelude (`pawn.js`) runs in the raw context scope, **not** inside the plugin's CJS `(function(require,module,exports){…})` wrapper, so it resolves `@s2script/entity` (post-5C.1; was `@s2script/std`) via the `__s2require` native, never a bare `require` (a live-gate finding).

**Slice 5B.3 complete — schema codegen (Slice 5B done):** `s2script gen-schema` (a pure offline transform over the committed `schema-catalog.json` + a hand-maintained curated class list `games/cs2/codegen-classes.json` = `CCSPlayerPawn`/`CCSPlayerController`/`CCSWeaponBase` + auto-included ancestor chains) generates typed field accessors — authors now write `pawn.health`/`pawn.friction` (idiomatic props: strip `m_`+Hungarian tag, camelCase; collision→raw fallback), and the raw `__s2_schema_offset`+`EntityRef.read*` plumbing is **internal** to the generated getters/setters (offsets still resolved **live**, never baked; setters auto-call `notifyStateChanged`; handle fields → `EntityRef`). Two **committed** artifacts — `games/cs2/js/schema.generated.js` (runtime, injected ahead of `pawn.js` via a `package-addon.sh` concatenation) + `packages/cs2/schema.generated.d.ts` (`export interface CCSPlayerPawn extends CCSPlayerPawnBase {…}`) — guarded by the deterministic `scripts/check-schema-generated.sh` freshness gate (regenerate + `git diff --exit-code`). **All codegen lives in the game-package/CLI layer — core untouched, so the live gate needed NO sniper rebuild.** Scalars + handle only (enum deferred — the catalog records enum without byte-width; `Vector`/strings/embedded/`ptr`/64-bit all skip-with-logged-reason). The 3-unit generator (pure `model` + `.d.ts`/`.js` emitters, all node:test-covered) lives in `packages/cli/src/schemagen/`. Proven live on de_inferno: `pawn.health`/`friction`/`clientSideRagdoll`/`teamNum`/`controller` read correct through generated accessors, all `null` on `bot_kick`, server ticking. **Slice 5C.1 complete — the module split:** the monolithic `@s2script/std` is dissolved into per-capability engine-generic packages (`@s2script/entity`←EntityRef, `/frame`←OnGameFrame+SubscribeOptions, `/timers`←delay/nextTick/nextFrame/threadSleep, `/console`←console, `/interfaces`←publishInterface+PublishHandle); **`@s2script/std` retired** (no umbrella — one import path per capability). One engine-generic rule does resolution: `s2require` maps any `@s2script/<name>` → `globalThis.__s2pkg_<name>` (game packages like `@s2script/cs2` use the SAME rule; core never hardcodes the module list; `loader.rs` builtin-skip is `starts_with("@s2script/")`); the CLI externalizes `@s2script/*` by wildcard; each module is a types-only package (`packages/<mod>/{package.json,index.d.ts}`). All consumers migrated in-slice (pawn.js, preludes, examples, fixtures, the codegen's emitted import + regenerated `schema.generated.d.ts`). Proven live on de_inferno: the demo imports `@s2script/frame`+`@s2script/cs2`, reads the 5B.3 generated accessors, degrades to null on `bot_kick`. **Slice 5C.2 complete — the player model:** `Player` **is** the CS2 controller (`CCSPlayerController`, the persistent player) — the generated accessors (`teamNum`/`score`/`ping`) applied to its prototype + `.slot` (0-based) + typed nav `player.pawn` (→`Pawn` via `m_hPlayerPawn`, shadows the raw generated handle) + `Player.fromSlot(slot)`/`Player.all()`; `Pawn` gains `pawn.controller` (→`Player` via `m_hController`). All `EntityRef`-backed, serial-gated `T|null`; a stored `Player` degrades to null on reuse (the safety CSSharp uses userid re-lookup for). **Occupancy (live finding):** CS2 pre-allocates all 64 controllers, so `isValid()` returns every slot and `m_iConnected` reads 0 for occupied+empty alike (diagnosed live); the clean schema signal is a **live player pawn** (`m_hPlayerPawn`), so `fromSlot`/`all` yield in-game/spawned players. Entirely in `games/cs2/js/pawn.js` + `packages/cs2/index.d.ts` (types `Omit<>` the shadowed handle fields) — **JS-only, no core/shim change, no sniper rebuild**. Proven live (`bot_quota 2`): `Player.all()`→the 2 bots (`slot=0 teamNum=2 health=100 backSlot=0`; `slot=1 teamNum=3 health=100 backSlot=1`), `players=0` on `bot_kick`. **Slice 5B.4 complete — string + 64-bit fields:** the typed reads + codegen extend to `char[N]` inline strings + 64-bit (`uint64`/`int64`/`float64`). New pure `entity.rs` helpers (`read_u64`/`read_i64`/`read_f64`/`read_string`) behind the kind-dispatch (`KIND_U64=9`/`I64=10`/`F64=11` → `v8::BigInt::new_from_u64`/`new_from_i64`/`set_double`) + a separate `__s2_ent_ref_read_string(idx,serial,off,maxLen)` native returning a **copied** `v8::String` (pointer never crosses to JS); `EntityRef` gains `readUInt64`/`readInt64` (→`bigint`), `readFloat64` (→`number`), `readString` (→`string`), all serial-gated `T|null`. **Key decision (user-confirmed): the low-level 64-bit PRIMITIVES return `bigint`, but the GENERATED accessors for `uint64`/`int64` return a DECIMAL STRING** (emit-js reads the bigint, null-guards, `.toString()`s) — SourceMod-parity (`GetClientAuthId(AuthId_SteamID64)` is a string; SourcePawn has no 64-bit int), **wire-safe** (strings cross the inter-plugin structured-copy wire → the `BigInt`-on-wire concern is now MOOT for field reads; no devalue ever needed), exact (no 2^53 loss). `char[N]`→string, `f64`→number. Codegen: `classifyField` maps `char[N]`→`{str,strLen}` + `uint64/int64/float64`→kinds; `TSTYPE` u64/i64→`string|null`; emit-js 3 getter shapes (str/u64|i64/default). A **name-transform fix** shipped (idiomaticName strips only a `KNOWN_TAGS` Hungarian set: `m_steamID`→`steamID` [was over-stripped `iD`], `m_bombSite`→`bombSite`; regenerating re-idiomaticized ~15 previously over-stripped fields, **no regression** to `health`/`teamNum`/`friction`/`pawn`/`controller`; `ui`/`ub`/`rt`/`ms` prefixes now conservatively KEPT). All codegen in the CLI/game-package layer; the natives needed **one sniper rebuild**. Proven live on de_inferno (`bot_quota 2`): `player.playerName`=`"Specialist"`/`"Rex"` (char[128]→string), `player.steamID`=`"0"` **`typeof string`** (uint64→decimal string), raw `readString(2036,128)`/`readUInt64(2528)` agree with the generated accessors; `players=0` on `bot_kick`, server ticking, no crash. **Slice 5C.3 complete — the Vector value type (first `@s2script/std`-breadth module):** a new engine-generic `@s2script/math` module — `Vector`/`QAngle` **value types** (`{x,y,z}` copied snapshots; `Vector.length()`) in the **core prelude** (`__s2pkg_math`, alongside entity/frame/…; a vector is engine-generic → NOT in `@s2script/cs2`) — plus a `__s2_ent_ref_read_floats(idx,serial,off,count)` native (`EntityRef.readFloats(off,count)`→`number[]|null`, one serial-gated lookup, `count`≤4-bounded, a copied array — pointer never crosses) + codegen for **direct atomic `Vector`/`QAngle` fields**. The GENERATED getter (game layer) constructs the value type inline (`var a=this.ref.readFloats(off,3); return a===null?null:new Vector(a[0],a[1],a[2])`); **`@s2script/entity` stays independent of `@s2script/math`** (the generated code requires math itself, conditionally + sorted for determinism). `classifyField` maps atomic `Vector`→`vector`/`QAngle`→`qangle` (fixed 3-float layout hardcoded in `VEC`; `Vector2D`/`Vector4D`/`Color`/`Quaternion` still skip). `pawn.eyeAngles` (QAngle) + `pawn.absVelocity` (Vector) + ~15 more direct fields now generate. A `Vector` is a plain object → crosses the inter-plugin wire cleanly (no wire caveat). Needed **one sniper rebuild** (prelude + native). Proven live on de_inferno (`bot_quota 2`): `pawn.eyeAngles`=`QAngle(pitch,yaw,roll)` real frame-varying view angles, `pawn.absVelocity`=`Vector(0,0,0)` (stationary bots — a correct read), `.length()`→speed; `players=0` on `bot_kick`, server ticking, no crash. **Slice 5C.4 complete — pointer-chain field navigation (`origin`):** a generic engine-generic `__s2_ent_ref_read_floats_chain(idx,serial,ptrOffs[],finalOff,count)` native follows a chain of pointer derefs **entirely in-core** (`read_ptr` per hop, serial-gated root, each hop null-checked; the raw intermediate pointers **never cross to JS**) and returns a copied float triple; guards (count/finalOff/non-array) fire BEFORE the resolve so they're in-isolate testable. `EntityRef.readFloatsChain(ptrOffs,finalOff,count)`. Hand-written `pawn.origin` (entity → `m_CBodyComponent`(ptr) → `m_pSceneNode`(ptr) → `m_vecAbsOrigin` Vector) + `pawn.angles` (`m_angAbsRotation` QAngle, distinct from `eyeAngles`=view/aim) in `pawn.js` — CS2 chain in the game layer, offsets live-resolved; mirrors the 5C.2 player nav (JS+types, no codegen regen). Needed **one sniper rebuild** (the native). Proven live on de_inferno (`bot_quota 2`): two bots at DISTINCT spawns — `slot=0 origin=Vector(-1662.18,288.76,-63.97)`, `slot=1 origin=Vector(2353,1977,135.52)` (real map coords, not zero/garbage/null), `angles`=plausible rotation; `players=0` on `bot_kick`, server ticking, no crash. **Slice 5D.1 — game events: MECHANISM + TYPED CATALOG complete; LIVE DELIVERY deferred to 5D.1b (a signature).** A generic engine-generic event bus: the shim registers an `IGameEventListener2` with `IGameEventManager2`, holds the live `IGameEvent*` (never crosses to JS) + calls `s2script_core_dispatch_game_event(name)`; the core notify-only event multiplexer (`core/src/event_mux.rs` — mirrors `OnGameFrame`: snapshot + `remove_by_owner` ledgered teardown) dispatches to per-name JS subscribers. `Events.on(name, handler)` + a live `GameEvent` accessor (`getInt`/`getFloat`/`getBool`/`getString`, `getUint64`→decimal string, `getPlayerSlot`→slot; **synchronous-only, block-scoped** — a stashed `ev` post-`await` reads defaults). 8 new `S2EngineOps` event ops (Rust `v8host.rs` + C `s2script_core.h`, ABI-matched, APPENDED after `schema_enumerate`) + `__s2_event_subscribe`/`unsubscribe`/`get_*` natives. Player resolution via `Player.fromSlot(ev.getPlayerSlot(key))` — events need NO engine-identity. **Typed IntelliSense overlay** (the user's "all event types" ask): reference-sourced `games/cs2/gamedata/event-catalog.json` (12 seed events — CS2 buries defs in the VPK, no live dump) → the `eventgen` codegen (`packages/cli/src/eventgen`, types-only, freshness-gated `check-events-generated.sh`) → `packages/cs2/events.generated.d.ts` = a `GameEvents` map + a typed `Events.on<K>` overload where each getter's `key` is constrained to that event's fields of the matching type; `pawn.js` re-exports `Events` into `__s2pkg_cs2`. Runtime stays the generic bus; uncatalogued events use the string API. **LIVE-GATE FINDINGS:** (1) FIXED — `CKV3MemberName(const char*)` (keying field reads) pulled in tier1's `MurmurHash2LowerCase` (undefined symbol → shim wouldn't `dlopen`); a **self-contained impl** (`shim/src/tier1_shims.cpp`, Valve's exact MurmurHash2 + ASCII lowercase) resolves it without the `tier1.a`/`CUtlString` cascade → the shim loads, core inits, the demo subscribes. (2) DEFERRED — **`IGameEventManager2` is NOT exported via `CreateInterface` in CS2** (neither the engine nor the server factory resolves `GAMEEVENTSMANAGER002`); it needs a **signature scan** (the CSSharp/Swiftly approach) — engine-RE + a per-update gamedata signature, deferred to **5D.1b**. Until then the event ops **degrade gracefully** (subscribe = no-op, no crash); the mechanism is proven in-isolate (T1 115 / T2 116 / T4 27 tests) — once `s_pGameEventManager` is non-null the existing listener + dispatch deliver events with NO further changes. **Slice 5C.5 complete — curated pointer-chain field navigation (codegen):** generalizes 5C.4's hand-written `pawn.origin` into codegen over a CURATED `games/cs2/nav-targets.json` (NOT graph traversal — the graph is 1918 embedded + 113 ptr fields, cyclic, mostly noise). A generic engine-generic `__s2_ent_ref_read_chain(idx,serial,pathOffs[],finalOff,kind)` native (`readFloatsChain`'s deref loop + `s2_ent_ref_read`'s `match kind` for the scalar final) + `EntityRef.*Via` methods (`readInt32Via`…`readHandleVia`→decode, **isValid-guarded like `readHandle`**); vectors reuse 5C.4 `readFloatsChain`. `navgen` (`packages/cli/src/navgen`, REUSES schemagen `buildModel`/`flattenedFields`/`classifyField`/`TSTYPE`) emits **pointer-backed wrappers** per curated target + nav accessors on the source; `games/cs2/js/nav.generated.js` (concatenated between `schema.generated.js` and `pawn.js`) + `packages/cs2/nav.generated.d.ts`, freshness-gated (`check-nav-generated.sh`). 4 targets: `pawn.sceneNode` (via `m_CBodyComponent`→`m_pSceneNode`→`CGameSceneNode`: `absOrigin`/`absRotation`/`scale`/…), `pawn.weaponServices` (→`CCSPlayer_WeaponServices.m_hActiveWeapon`→`EntityRef`), `pawn.movementServices` (`ducked`/`ladderNormal`/…), `pawn.aimPunchServices`. **Safety:** a wrapper holds `(rootRef, pathOffsets)` — never a cached pointer; every field access re-resolves the chain in-core serial-gated at the root → stale→null; **and `applyNav` re-resolves the path OFFSETS per access** (self-healing across the schema-warm boundary — the boot-window discipline; a T3-review fix). `VectorWS`→vector. 5C.4 `pawn.origin`/`angles` kept as thin aliases → `sceneNode.absOrigin`/`absRotation` (idiomaticName strips `vec`/`ang` but keeps `Abs`). Needed **one sniper rebuild** (the native). Proven live on de_inferno (`bot_quota 2`): `pawn.sceneNode.absOrigin`=two distinct spawn coords, `scale`=1, `pawn.weaponServices.activeWeapon`=`ref#1014`/`#1013` (valid weapon EntityRefs), `pawn.movementServices.ducked`=false, the `origin` alias `ok`; `players=0` on `bot_kick`, server ticking, no crash. **Slice 5D.2 complete — engine-backed access: live game events + engine identity (the engine-RE bundle DONE).** Two threads over one gamedata spine (`gamedata/core.gamedata.jsonc`), both feasibility-proven offline by RE spikes on the pinned `libserver.so`/`libengine2.so` (BuildID `d41373…`) then proven LIVE. **Thread A — live game-event delivery (the deferred 5D.1b):** a pure engine-generic byte-pattern scanner (`shim/src/sigscan.{h,cpp}`, host-`g++`-tested `ParsePattern`/`FindPattern`/`ResolveLeaDisp`/`ResolveCtorXref`) resolves the un-exported `IGameEventManager2*` global from `libserver.so` via a committed **ctor-body-xref signature** + a `.signatures` gamedata section + `LoadSignatures` — populating `s_pGameEventManager` (replacing the always-MISSING `GAMEEVENTSMANAGER002` factory that 5D.1 confirmed). ALL of 5D.1 downstream (listener, `event_mux`, ops, natives, typed catalog) is unchanged. **Thread B — engine identity:** 5 engine-generic client-list ops (`client_valid`/`userid`/`signon`/`name`/`find_by_userid`) APPENDED to `S2EngineOps` (C header + Rust mirror, ABI order); the shim reads `INetworkServerService*` (now stored) → game server → `CServerSideClient[]` at **committed gamedata offsets** (`NetworkServerService.gameServer`=336, `NetworkGameServer.clientCount/Elems`=592/600, `ServerSideClient.name/signon/userId`=64/100/168); CS2 JS adds `player.userId` (engine, not schema), `Player.fromUserId` (pawnless-safe), `Player.allConnected()` (connected-but-pawnless enum, additive to the pawn-gated `Player.all()`). Boundary held (`CServerSideClient`/`IGameEventManager2`/`INetworkServerService` are Source2 ENGINE types → shim-only; core stays generic, both gates green). Needed **one sniper rebuild** (shim + the client natives). **LIVE-GATE bug found + fixed:** `FindModuleText("libserver.so")` first grabbed **Metamod's thin `libserver.so` proxy** (`csgo/addons/metamod/…`, ~95 KB, no game code) because its path also contains the substring — the original `return 1` stopped at the first match; **fixed** to scan ALL substring matches and keep the **largest** PF_X segment (the real ~25 MB module). Proven live on de_inferno (`bot_quota 2`): `interface OK: GameEventManager (sig-scan ctor-body-xref, 0x…e0860)`; events deliver (`EVENT player_spawn slot=0/1`, `EVENT round_start timelimit=0` — first-ever 5D.1 delivery); `allConnected=2`, `slot=0 userId=0 teamNum=2 pawn=yes fromUserId(uid).slot=0` / `slot=1 userId=1 …slot=1` — **`fromUserId` round-trips to the same slot, confirming `clientElems[slot]` index == player slot** (soft-point resolved; `kSignonConnected=2` correct); `allConnected=0` on `bot_kick`, server ticking, no crash. See [[cs2-game-events-acquisition]] + the 5D.1b/5D.2 spike-findings + design/plan under `docs/superpowers/`. **Deploy note (treadmill):** `package-addon.sh` `rm -rf`s the bind-mounted addon dir → `docker compose restart cs2` re-binds it AND preserves the `gameinfo.gi` metamod patch; avoid `--force-recreate` (it resets `gameinfo.gi` → re-run `/patch-gameinfo.sh`). **Slice 5D.3 complete — event actionability (pre-hooks: block + modify, + firing).** The write direction for game events, at parity with the `OnGameFrame` multiplexer. The shim `SH_ADD_HOOK`s the sig-scanned `IGameEventManager2::FireEvent` (5D.2's `s_pGameEventManager`); a Pre hook runs the JS `Events.onPre` subscribers, collapses their `HookResult`s via the EXISTING `core/src/multiplexer.rs::run_chain` (the event mux was the only multiplexer without collapse — now at parity via a sibling `EVENT_MUX_PRE` + `dispatch_game_event_pre`), and on `Handled`/`Stop` **suppresses the client broadcast** via `SH_CALL(FireEvent)(ev, /*bDontBroadcast=*/true)` + `MRES_SUPERCEDE` (SM parity — the server still processes it, so `Events.on` post-subs still fire). **Modify:** `ev.setInt`/`setFloat`/`setBool`/`setString`/`setUint64` write the live `IGameEvent` during a pre-hook. **Fire:** `Events.fire(name, fields, dontBroadcast?)` = `CreateEvent` + typed `Set*` (runtime type-infer: bool/string/bigint→uint64/int/float) + `FireEvent`; the 5 set ops share ONE unified write target (`s_currentEvent`, save/retarget on `event_create`, restore on `event_fire`). 7 ops ABI-appended after the 5D.2 client ops (C header + Rust mirror); `HookResult` (`{Continue:0,Changed:1,Handled:2,Stop:3}`) already existed as a prelude global (re-exported from `@s2script/events`); a JS handler returning a `HookResult` is a new convention (`OnGameFrame` handlers return void). Engine-generic mechanism (core/shim; `IGameEventManager2` is a Source2 type); the typed `Events.onPre<K>`/`fire<K>` overlay (eventgen emit-dts, freshness-gated) is CS2-only. Needed **one sniper rebuild**. **LIVE-GATE defect found + fixed:** `Events.fire` from inside a handler re-enters the dispatch while the isolate is already `HOST.borrow_mut()`'d → `RefCell already borrowed` panics (caught, but logged + lost the nested re-dispatch); **fixed** with a `try_borrow_mut()` graceful-skip in both event-dispatch paths (engine-side fire still happens; nested JS re-dispatch skipped — pre→allow, notify→no-op; test `reentrant_dispatch_skips_gracefully_no_panic`). **Documented limitation:** a JS-triggered `Events.fire` cannot re-dispatch to JS subscribers (all JS runs under the isolate borrow) — the fired event reaches the engine (clients + C++ listeners) but not our own `on`/`onPre` subs on that pass. Proven live on de_inferno: `Events.fire(...)`→`ok=true`; `onPre("round_start")` sets `timelimit`→`4242` + `Handled`, the POST subscriber reads the modified `4242` (modify + block-as-broadcast-suppress + the FireEvent vtable index all confirmed), 0 panics. See `docs/superpowers/specs/2026-07-03-slice-5d3-*`. **Slice 5E.1 complete — the `tsc` typecheck gate.** `s2script build` now typechecks each plugin (full `strict`) against the shipped engine `.d.ts` and FAILS the build (emits no `.s2sp`) on any type error — closing the charter's "typecheck-gate every load/reload" at the build/reload edge (the load-time apiVersion refuse already lives in `loader.rs`; a failing dev-reload rebuild produces no `.s2sp`, so the running plugin is untouched — for free). Mechanism: `packages/cli/src/typecheck/typecheck.ts::typecheckPlugin(dir,{packagesDir})` runs the **TypeScript compiler API in-process** (`--noEmit`, `strict`, `skipLibCheck`, `lib: es2020`, `types: []` — the sandbox, NOT `lib: dom`), resolving `@s2script/*` via `paths → packages/*/index.d.ts`, the injected global `console` via a shipped **`packages/globals/globals.d.ts`** (a script `.d.ts`), and each **non-`@s2script`** `pluginDependency` via a temp ambient `declare module "<dep>";` (`any` — full inter-plugin interface typing DEFERRED). `buildPlugin(dir, packagesDir)` typechecks FIRST → throws formatted `file:line:col — TSxxxx` diagnostics → no archive. `typescript` is a new (externalized) CLI dep; node:test-covered (hermetic fake-packages + clean/broken fixtures). **Plugins are authored in PURE ESM** — `module: ESNext` REJECTS `import x = require(...)` on purpose (the two `import = require` examples were converted to ESM named imports, which esbuild bundles to the identical `require("dep").member` access — runtime-unchanged; `import * as` is avoided since its `__toESM` interop can't enumerate a get-only interface Proxy). **The examples are the forcing function** (all six pass full strict → validates the `.d.ts` surface): the one real bug it surfaced was an ambient-stub collision — a plugin listing `@s2script/frame`/`@s2script/interfaces` in `pluginDependencies` got a `declare module "@s2script/interfaces"` (`any`) stub that SHADOWED the real `.d.ts` (→ `PublishHandle` seen as a namespace / `@s2script/cs2` degraded to `any`); fixed by filtering `@s2script/*` out of the ambient stubs (they resolve via `paths`). Once fixed, the typed `Events.on<K>`/`onPre<K>` overloads infer the event type with NO annotations. `scripts/check-plugins-typecheck.sh` joins the `check-*` gate suite. NO Docker live gate (a build-tool behaviour). **Slice 5E.2 complete — plugin config materialization (first of the lifecycle sub-slices).** Plugins DECLARE typed config in `package.json` (`s2script.config` = `{key:{type,default,description?}}`; types `string`/`int`/`float`/`bool`; the CLI validates `default` matches `type` at build + bakes the block into the `.s2sp` manifest — `validateConfigBlock`); the host MATERIALIZES at load = declared defaults ⊕ an admin-editable JSON override file `addons/s2script/configs/<sanitized-id>.json` (AUTO-GENERATED on first run as JSONC with sorted keys + `//`-comments; per-key degrade — a wrong-typed/malformed value → default + WARN, never a crash); the plugin READS via a new engine-generic `@s2script/config` (`getString`/`getInt`/`getFloat`/`getBool`, undeclared → type zero-value, never throws). **Live-reload is OPT-IN** ("the author decides" = presence of a `config.onChange(handler)` sub): no sub → materialized once at load, file not watched (zero overhead); the FIRST `onChange` makes the loader's frame-drain poll watch that plugin's file (content-string compare, baseline SEEDED at watch-time so no spurious initial fire) → on change re-materialize + re-inject `__s2pkg_config_values` + fire handlers, **no plugin reload**. The pure `core/src/config.rs` (`materialize_config`/`generate_default_jsonc`, V8-free, 5 tests) + `config_read`/`config_write` shim ops (ABI-APPENDED after the 5D.3 event ops, C header + Rust mirror) + `materialize_for_load`/`re_materialize_config` + the `CONFIG_SUBS` mux + the `__s2_config_on_change` native; the onChange fire loop MIRRORS `dispatch_game_event` (snapshot-release, `try_borrow_mut` re-entrancy guard, `REGISTRY.is_live` liveness, per-sub TryCatch); `config_decls` stored on `PluginInstance` via `store_config_decls` at both loader sites (no signature churn). Engine-generic throughout (both boundary gates green); `packages/config` is a types-only `.d.ts`. Needed **one sniper rebuild** (the shim ops + prelude). **LIVE-GATE findings (both FIXED):** (1) the `@s2script/config` module registered as `__s2pkg_config = __s2_config` but the `.d.ts` declares a NAMED `config` export, so the demo's `import { config }` (→ `require(...).config`) read `undefined` → `onLoad` TypeError; **fixed** to `__s2pkg_config = { config: __s2_config }` (the in-isolate test had missed it by reading the module object directly — updated to go through `.config`). (2) the `.s2sp` addon is bind-mounted `:ro` (host owns the plugins dir) but auto-generate needs CONTAINER writes to `configs/`; **fixed** with a nested read-write mount `../dist/addons/s2script/configs:…/configs` layered over the `:ro` parent (Docker resolves by longest target path) — a production deploy must keep `configs/` writable. Proven live on de_inferno: first load AUTO-GENERATED `_demo_hello.json` (defaults + `//`-comments), `[demo] onLoad — greeting=hello from s2script maxUses=3 enabled=true`; editing the file → `[demo] config changed — greeting=live-reloaded via onChange maxUses=7 enabled=false` with **no 2nd onLoad** (no reload); corrupting it → re-materialized to defaults, `RestartCount=0`, server ticking, no crash. See `docs/superpowers/specs/2026-07-03-slice-5e2-*`. **Slice 5E.3 complete — reload state-handoff (second lifecycle sub-slice).** A hot-reloaded plugin carries runtime state from its old instance to its new one: on a same-id file-watch **Reload**, the old instance's `onUnload(): State` return is held across the teardown→load gap and passed to the new instance's `onLoad(prev)`. **Mechanism REUSES the inter-plugin marshalling verbatim** — `unload_plugin` serializes the `onUnload` return in the OLD context via `iface_to_json` (`JSON.stringify` + the `__s2_entref_replacer`; non-serializable → `None` + WARN) into a host-side `PENDING_HANDOFF: thread_local HashMap<String,String>` (a plain `String` → survives the context's disposal); `load_plugin_js` consumes it via `iface_from_json` (`JSON.parse` + the `__s2_entref_reviver` → **live serial-gated EntityRefs** rebound to the NEW context) and passes it as `onLoad`'s single arg — **consume-once** (removed regardless of revival/throw). So a `State` may hold any JSON value (primitives/strings/arrays/nested objects) + **`EntityRef`** (revives live; `isValid()===false` if the entity died in the gap, never a crash); a 64-bit value is carried as a decimal string (a `bigint` can't be `JSON.stringify`d → would silently drop the whole handoff, the same reason 5B.4 accessors return strings). The loader orchestrates: **Reload** = `unload`(captures)→`load`(consumes); **Vanished** (final removal) = `unload`→`clear_pending_handoff` (discard); first **Load** → `onLoad(undefined)`; `shutdown` resets the map. **Degrade-never-crash:** `onUnload` throws / non-serializable → no handoff + WARN; `onLoad(prev)` throws → the existing WARN; nothing throws into the engine. **ENTIRELY engine-generic core** (`v8host.rs` capture/revive + `PENDING_HANDOFF`; `loader.rs` Reload-consume vs Vanished-clear) — **no shim/native/op change, no new serializer**; one sniper rebuild (core `.so` only), both boundary gates green. `onLoad`/`onUnload` stay documented free-function conventions (no `@s2script/lifecycle` types package). Proven live on de_inferno: first load `[demo] onLoad — reloads=0 hadPrev=false pawnAlive=null`; two touches (Reload) → the counter **CLIMBS** `reloads=1`/`reloads=2 hadPrev=true` AND a tracked pawn `EntityRef` **survives the gap LIVE** (`pawnAlive=true pawnRef=732/69664` — the reviver rebound it serial-gated, validating against the shared entity system); delete (Vanished) → re-add (Load) → `reloads=0 hadPrev=false` (pending cleared, fresh identity); `RestartCount=0`, server ticking, no crash. See `docs/superpowers/specs/2026-07-03-slice-5e3-*`. **Slice 6 STARTED — the base-plugin suite (the std-lib acceptance test / north star). Decomposition (user, spine-first): 6.1 command spine + chat → 6.2 admin cache + `ADMFLAG_*` flags → 6.3 target-resolution + kick + slap. (Plugin-capability *permissions* — my earlier 5E.4 idea — was DROPPED as premature + non-SM-parity: SM plugins run with full ungated API access; SM's "permissions" = admin command FLAGS, which belong in 6.2/6.3, not a plugin-manifest capability gate.) Slice 6.1 complete — the command spine (chat SEND deferred to 6.1b).** A plugin registers a server command + gets a typed dispatch ctx; a `@s2script/chat` module carries messages to player chat. **Engine-generic `@s2script/commands`** — `Commands.register(name, handler)`; the handler's `ctx` = `{callerSlot (-1=server console), args, argString, reply}` (a raw slot → the module never depends on `@s2script/cs2`; `reply` routes console-vs-player-chat). `CONCOMMANDS` is owner-tracked; `dispatch_concommand` runs the handler in the REGISTERING plugin's context (liveness-gated + `try_borrow_mut` re-entrancy guard, mirrors `dispatch_game_event`) + drops a plugin's commands on unload. **Engine-generic `@s2script/chat`** — `Chat.toSlot`/`toAll` (loops live slots via `__s2_client_valid`, NOT `Player.all()`) over a new `client_print` op + `__s2_client_print` native (ABI-appended after `config_write`). **The cracked RE blocker:** CS2 does NOT export `ConCommand::Create` (the seed was neutralized on that); registration works via `ICvar::RegisterConCommand(ConCommandCreation_t&)` — a VTABLE call on the already-resolved `VEngineCvar007` (the shim fills a `ConCommandCreation_t` whose callback is the existing trampoline + stores the `ConCommandRef` in a name-keyed map = name-lifetime anchor + idempotent reload-safe). Command delivery for the gate uses the existing `scripts/rcon.py` (the Slice-0 Source RCON client). Boundary held: core engine-generic (`@s2script/commands`+`/chat`), shim owns the CS2 registration + the (stubbed) chat send; the base plugin (`@s2script/basecommands`) is CS2. **Slice 6.1b/c — `client_print` delivers to the client CHAT BOX (`SayText2`):** `Chat.toSlot`/`toAll` → `client_print` sends a `CUserMessageSayText2` — `AllocateMessage()` the game's protobuf object, set `entityindex`/`chat`/`messagename` via **protobuf REFLECTION**, `IGameEventSystem::PostEventAbstract` to the slot. **The key:** LINK the vendored `libprotobuf.a` (`hl2sdk/lib/linux64/release/`, protobuf 3.21.8 matching the game) — it's **self-contained** so it does NOT reintroduce the tier1 `dlopen` cascade (5D.1; my earlier "reflection breaks dlopen" fear was WRONG — I'd assumed I couldn't LINK protobuf, but the static `.a` is right there). Reflection then works on the game's message (same ABI) — NO `protoc`/generated headers. Message found via `FindNetworkMessagePartial("SayText2")` (exact `FindNetworkMessage` missed it; descriptor full_name is `CUserMessageSayText2`, 7 fields). **Bots skipped** — a send to a fake client (no netchannel) can crash (live-gate finding: an un-guarded engine print on a bot segfaulted the server); guard on `IVEngineServer2::GetPlayerNetInfo(slot)!=null` (SM's fake-client skip). Proven live: shim dlopens w/ libprotobuf linked (`Loaded 1 plugin`), the SayText2 descriptor + all 3 target fields resolve, `sm_say` dispatches, `RestartCount=0`, no crash. **Caveat:** a REAL client visually SEEING the chat line is unverifiable on the bots-only gate (no human client) — but message construction, field-resolution, and the send path are all proven live. `leak-TODO`: `pData` ownership after `PostEventAbstract` unconfirmed (no `DeallocateMessage` seen) — not freed to avoid double-free. Needed **one sniper rebuild** (the registration + interface resolution; the ABI `client_print` op was T1). Proven live on de_inferno: `interface OK: EngineCvar`; `ConCommand 'sm_say' registered (accessIdx=823)`; `scripts/rcon "sm_say hello world"` → `[basecommands] sm_say by slot=-1 msg=hello world` (register→trampoline→owner-context dispatch→ctx→handler ALL live); `Chat.toAll` looped the two live bot slots (send stubbed). See `docs/superpowers/specs/2026-07-04-slice-6.1-*`. **6.1 TODO for the base suite (final-review Minor):** `CONCOMMANDS` is keyed by name only, not `(owner, name)` — a cross-plugin same-name registration is silent last-writer-wins (B overwrites A; B's unload orphans it for still-loaded A). Fine for 6.1 (one base plugin) but plan owner-scoped ownership / conflict detection before 6.2/6.3 where clashes get likely. **Slice 6.2 complete — admin cache + flags (the real SM "permissions").** A HOST-GLOBAL two-tier admin cache in CORE (`ADMIN_FILE` ⊕ `ADMIN_RUNTIME`, SteamID64→u64 mask; must be core — V8 contexts isolated, so a runtime `Admin.add` in plugin A must be visible to plugin B's gating) + 6 natives (`__s2_admin_set/get(union)/remove/clear_file/mark_loaded`, `client_steamid` op via `IVEngineServer2::GetClientXUID`). **Engine-generic `@s2script/admin`** — `ADMFLAG` (SM bit values, `ROOT`=1<<14⇒all; `hasFlags(req)=(f&ROOT)||(f&req)===req`) + `Admin.add/remove/get/forSlot/reload` + `admins.json` parse (JSON `{steamid:[names]}`→bits) / one-shot auto-gen+load (via `config_read/write("admins")` REUSE + JS-bridge natives `__s2_config_read_raw/write_raw`) + installs `globalThis.__s2_admin_check` hook; `player.steamId`. **The SM command TRIO in `@s2script/commands`** — `register`(console/anyone, 6.1) / `registerServer`(callerSlot<0 only) / `registerAdmin(name,flags,handler)` (callerSlot<0=root; else consult the hook; **no-hook→deny+warn-once fail-safe**, never accidentally grant); `@s2script/commands` stays admin-AGNOSTIC (only the global hook, no admin import). Retrofit: `sm_say`→`registerAdmin(ADMFLAG.CHAT)`. Boundary held (core cache is engine-generic; `player.steamId` in the CS2 layer). Needed **one sniper rebuild** (the `client_steamid` shim). Proven live on de_inferno (rcon): `admins.json` auto-generated as VALID JSON (a `_help` template — a 2nd restart re-reads it with NO "malformed" warn; a `//`-comment template would've failed `JSON.parse` — a T2-review fix); `runtime-add hasKick=true hasBan=false` (Admin.add+get+hasFlags live); `slot0=not-admin (bot/steamid=0)` (SteamID read + lookup); `python3 scripts/rcon.py "sm_say …"` (server console=root) → `sm_say by slot=-1` runs; `RestartCount=0`. **DEPLOY note:** the `admins.json` container-write needs `dist/addons/s2script/configs` to be a host-WRITABLE dir (the 5E.2 nested rw mount) — after a sniper build wipes `dist/addons/s2script`, recreate `configs/` as gkh BEFORE restart, else the container (`steam` uid) can't write it (a live-gate finding). See `docs/superpowers/specs/2026-07-04-slice-6.2-*`. ****Slice 6.3 complete — target resolution + sm_kick + sm_slap (the base-command action side; Slice 6 trio done).** The first real player *actions* + SourceMod target-string resolution. **`Player.target(pattern, callerSlot) → Player[]`** (CS2 layer, `pawn.js`) resolves the core SM selector set: `#<userid>`→`fromUserId`; `@all`→`allConnected`; `@me`→`fromSlot(callerSlot)` (empty from the server console, `callerSlot<0`); else a **case-insensitive `playerName` match — exact wins over partials, else all partials**; empty on no match. **`sm_kick <target> [reason]`** (`ADMFLAG.KICK`) via ONE new **engine-generic `client_kick(slot, reason)` op** (ABI-appended after `client_steamid`; `__s2_client_kick` native degrades no-op) → the shim's `IVEngineServer2::KickClient(CPlayerSlot, reason, NETWORK_DISCONNECT_KICKED)`; `player.kick(reason?)` wraps it. **`sm_slap <target> [damage]`** (`ADMFLAG.SLAY`) = **reliable damage** (`pawn.health = max(1, hp-damage)` via the 5B.3 generated setter — `writeInt32`+`notifyStateChanged`) + **best-effort knockback** (`pawn.setVelocity(x,y,z)` = `writeFloat32`×3 on `m_vecAbsVelocity` + `notifyStateChanged`, no new native; the slice does NOT depend on knockback surviving physics). Boundary held: the op is `(slot, reason)` — engine-generic; CS2 field names (`m_vecAbsVelocity`) stay in the game layer. **Needed one sniper rebuild** (the `client_kick` op + shim). Proven live on de_inferno (`bot_quota 2`, rcon): `sm_slap Rex 10` → `slot=1 hp 100→90`; `sm_slap @all 5` → both bots (95 / **90→85, Rex's hp PERSISTED across commands** = a real, sticky state write); `sm_slap nobody` → "No matching players"; `sm_kick #1 test-kick` → engine `Rex kicked by Console (test-kick)`, "Kicked 1 player"; `sm_kick @all cleared` → both disconnected (engine-confirmed); `sm_kick nonexistent` → "No matching players"; `RestartCount=0`, server ticking, no crash. See `docs/superpowers/specs/2026-07-04-slice-6.3-actions-design.md`. **Current focus: Slice 6.3 DONE — the SM base-command trio (sm_say/sm_kick/sm_slap) with admin gating + target resolution is complete.** **Slice 6.4 complete — server-command primitive + `sm_map`.** A new engine-generic **`@s2script/server`** module — **`Server.command(cmd)`** (→ `IVEngineServer2::ServerCommand`, queued) + **`Server.isMapValid(map)`** (→ `IsMapValid`) — over **two ops (`server_command`/`server_map_valid`) ABI-appended after `client_kick`** (C header + Rust mirror + both test literals; natives degrade to no-op/`false`; shim passthroughs `!s_pEngine`-guarded). One op-pair unblocks a whole class of SM admin commands (`sm_cvar`/`sm_exec`/`sm_rcon` are trivial follow-ons — deferred). **`sm_map <mapname>`** (`ADMFLAG.CHANGEMAP`, in `@s2script/basecommands`): sanitize `^[A-Za-z0-9_]+$` (injection guard, we build `"changelevel <map>"`) → `isMapValid` reject → `Server.command("changelevel "+map)`. Boundary held: the ops take a command string / map name (engine-generic); `sm_map` is CS2 basecommands. **One sniper rebuild.** Proven live on de_inferno (rcon): `sm_map foo.bar`→"Invalid map name" (sanitize), `sm_map notarealmap`→"'notarealmap' is not a valid map" (isMapValid), map unchanged after rejects; **`sm_map de_dust2`→ the map CHANGED (de_inferno→de_dust2), the plugin RELOADED at the new map-live (sm_map re-registered + onLoad after the changelevel), `RestartCount=0`, no crash**, `sm_say` live on the new map. (Live-gate note: an rcon `sm_map a;quit` runs `quit` — the Source CONSOLE splits on `;` BEFORE any ConCommand, identical to SourceMod; the handler only ever received `sm_map a`. Not a plugin flaw; the in-code sanitize still guards a `;` that arrives as a single arg token.) See `docs/superpowers/specs/2026-07-04-slice-6.4-server-command-design.md`. **Current focus: Slice 6.4 DONE — the base admin/player command suite now spans say/kick/slap/map; `sm_cvar`/`sm_exec`/`sm_rcon` (all on `Server.command`), `sm_slay`/`sm_ban`/`sm_gag`, and non-event pre-hooks remain the deferred queue.** **Slice 6.5 complete — admin-utility commands (`sm_who`/`sm_rcon`/`sm_exec`), JS-only (NO new op, NO core/shim change, NO sniper).** All three canonical SM basecommands on already-proven primitives: **`sm_who`** (`ADMFLAG.GENERIC`) lists `Player.allConnected()` + per-player admin status via `Admin.forSlot`; **`sm_rcon <command>`** (`ADMFLAG.RCON`) = a deliberate full `Server.command(argString)` passthrough (highest-trust, no sanitize — SM parity); **`sm_exec <cfgfile>`** (`ADMFLAG.CONFIG`) sanitizes the name (`^[A-Za-z0-9_./-]+$` + reject `..`) → `Server.command("exec "+file)`. Entirely in `@s2script/basecommands`; deploy was a `.s2sp` re-drop + restart (no rebuild). Proven live on de_dust2 (`bot_quota 2`, rcon): `sm_who`→`#0 Kev slot=0 steamid=0 admin=no` / `#1 Krikey …`; `sm_rcon echo s2-rcon-effect-proof`→the engine ECHOED it (definitive Server.command effect); `sm_exec server`→engine `execing server`, `sm_exec ../secret`→"Invalid config name"; `RestartCount=0`, no crash. See `docs/superpowers/specs/2026-07-04-slice-6.5-admin-utils-design.md`. **Current focus: Slice 6.5 DONE — the base admin toolkit spans say/kick/slap/map/who/rcon/exec. The remaining SM commands each need a NEW engine primitive with real RE: `sm_cvar` get/set (a ConVarData type-union read at gamedata offsets), `sm_slay`/`sm_burn`/`sm_freeze` (a bot-safe player-effect — engine `ClientCommand` on a fake client risks the `ClientPrintf` null-netchannel crash, so a game-function call is needed), and non-event pre-hooks (`OnTakeDamage`, a sigscan+SH_ADD_HOOK like 5D.3).** **Slice 6.6 Stages 1+2 complete — damage pre-hooks (the SDKHooks-equivalent `OnTakeDamage`).** The write direction for damage. **A new self-contained x86-64 Linux inline-detour engine** (`shim/src/detour.cpp` + the vendored HDE length-disassembler `third_party/hde/`; NO third-party hook lib, NO dlopen cascade — user chose hand-roll+hde64 after MinHook turned out Windows-only): overwrites a function prologue with a 14-byte absolute jump, relocates the stolen (position-independent) prologue into an mmap'd trampoline, refuses on any relative/rip-relative/undecodable instruction, restores on Unload. It detours **`CBaseEntity::DispatchTraceAttack`** (the CS2 damage entry — a NON-interface internal fn, so a detour not a SourceHook; unique prologue signature in `gamedata` `.signatures`, `resolve:"direct"`, validated unique in the pinned `libserver.so`; ModSharp uses the same fn as its sole damage hook). **`CTakeDamageInfo` is schema-registered** (`m_flDamage`@68/`m_hAttacker`@60/…) so read+**modify** reuse the live schema path — NO struct RE. The core `damage_mux` (mirrors `event_mux`) + `dispatch_damage` + the engine-generic **`@s2script/damage`** module — **`Damage.onPre(handler)`** + a block-scoped **`DamageInfo`** (`damage` get **and** SET [modify; set 0 = block], `damageType`, `attacker`/`inflictor`→`EntityRef`) — 3 ABI-appended ops (`damage_read_float`/`read_int`/`write_float`) read/write the live info at a schema offset (raw ptr never crosses to JS). `CTakeDamageInfo` is Source2-generic so it stays in core (boundary green). **Proven live end-to-end via a synthetic frame-300 self-test** (combat is un-generatable on this maxplayers server — 10 bots sit idle; the detour DIVERSION was proven by a Load-time sentinel self-test, then the full pipeline by a fake `CTakeDamageInfo` `m_flDamage=42`): `detour→core mux→new DamageInfo()→the demo Damage.onPre→info.damage reads 42→handler halves it (modify path)`, `RestartCount=0`. Needed sniper rebuilds. **Follow-up DONE (931544a):** `DamageInfo.victim` (`this`→`CEntityInstance::GetRefEHandle().ToInt()`→`EntityRef`, proven live `victim=0/1`) + core in-isolate tests (`damage_dispatch_runs_subscriber` + the natives-degrade asserts; 140/0) — so `DamageInfo` = damage(read+modify)/damageType/attacker/inflictor/victim. **Still deferred:** the real-bullet e2e + the a2-vs-a3 `CTakeDamageInfo`-arg confirm (need a human client — bots won't fight here, `CS2_MAXPLAYERS` bumped 2→12 but the frame-300/900/1800 **synthetic self-test** is the DEV diagnostic that proved the pipeline — now **GATED behind `S2_DAMAGE_SELFTEST=1`** (default OFF so production plugins' `Damage.onPre` never get fake fires; the dev compose sets it)); a `HookResult`-style block (skip original) vs the current `setDamage(0)`-block. See `docs/superpowers/specs/2026-07-04-slice-6.6-damage-hooks-spike.md`. Pick one to invest in next.** **Slice 6.7 complete — `sm_cvar` (cvar get/set) + a tier1-free cvar-read primitive.** The GET reads a `ConVarData` value **without any tier1 method call** (the clean `ConVarData::ValueOrDefault` / `ConVarRefAbstract::GetString`→`CUtlString` are NON-inline → they'd reintroduce the 5D.1 `dlopen` cascade): `ICvar::FindConVar`+`GetConVarData` (vtable on `s_pCvar`) + `GetType()` (inline) + a **direct `m_Values` read** (it's `ConVarData`'s last field → offset = `sizeof(ConVarData) - sizeof(CVValue_t)*MAX_SPLITSCREEN_CLIENTS`), formatted by `EConVarType` (bool/int16/uint16/int32/uint32/int64/uint64/float32/float64/string via the inline `CUtlString::Get`; Color/Vector/Qangle→`"<type>"`). A new `cvar_get` op (ABI-appended after `damage_victim`) → `@s2script/server` gains **`Server.getCvar(name)`** (the op) + **`Server.setCvar(name, value)`** (a `Server.command` wrapper — the console handles any type). **`sm_cvar <name> [value]`** (`ADMFLAG.CONVARS`, in `@s2script/basecommands`): no value→GET; with a value→SET then echo (NOT a read-back — `ServerCommand` queues the set for next frame, so an immediate `getCvar` reads the OLD value; the SET echoes the requested value). Name sanitized (`^[A-Za-z0-9_]+$`). Engine-generic (a cvar name→string is Source2). One sniper rebuild + core degrade test (140/0). **The SDK-layout-match RISK (the pinned hl2sdk can lag Valve) is LIVE-VERIFIED:** proven on de_dust2 (rcon): `sm_cvar sv_gravity`→**`800`** (the known default confirms the read is correct, not garbage), `mp_maxrounds`→`24` (int), `sv_cheats`→`0` (bool), `hostname`→`s2script-slice0` (string), `sm_cvar sv_gravity 500`→`set to 500` + a follow-up GET confirms `500` stuck, `sm_cvar notarealcvar`→empty, `RestartCount=0`. Deferred: a SYNCHRONOUS cvar SET (a ConVarData write, so the read-back is live); typed `getCvarInt/Float`; protected/cheat-cvar guards; Color/Vector formatting. See `docs/superpowers/specs/2026-07-04-slice-6.7-cvar-design.md`. **First PUSH to origin this session** (all prior work was local; the user authorized `git push origin main`).** Deferred TODOs (do NOT build ahead): team/`@ct`/`@t`/`@bots`/`@alive` target filters + multi-target disambiguation; proper damage-with-events (`OnTakeDamage`/armor — slap is a direct health write); lethal `sm_slay`; `sm_ban`/`sm_gag`/`sm_mute`; teleport / velocity that survives physics (an engine impulse call) / `Vector` writes; admin immunity checks. Caveat carried: a real non-admin player VISUALLY denied needs a human client (gating logic fully in-isolate tested); a real client SEEING SayText2 chat likewise. Also outstanding from "do em all": re-entrant `Events.fire` re-dispatch + non-event pre-hooks (`OnTakeDamage`/entity IO). `libprotobuf.a` links cleanly → other usermessages (hud/hint/centertext) unblocked.** Deferred (do NOT build ahead): full inter-plugin interface typing (producer-emitted consumer `.d.ts`); a runtime/load-time tsc; per-plugin tsconfig customization. Deferred (do NOT build ahead): blocking/pre-hooks/`HookResult` for events + *firing* events + an auto-dumped event catalog (VPK/KV3 or runtime-RE) + an eager event snapshot; graph auto-traversal / non-curated nav targets / cyclic-or-deeper chains / `CUtlVector` fields (`m_hMyWeapons`) + the quantized `m_vecOrigin` wrapper; Vector/origin **writes** (teleport = engine `Teleport()`) + `Vector2D`/`Vector4D`/`Color`/`Quaternion`; `CUtlString`/`CUtlSymbolLarge` strings + field **writes**; `enum` codegen + typed wrappers for handle-referenced classes; permissions (the last lifecycle sub-slice); the registry/platform (Slice 5.5); the base-plugin suite (Slice 6). **5E.2 fast-follow TODO:** deleting a watched config file at runtime fires `onChange` twice + resurrects the file (auto-generate ⊕ content-diff watch interaction) — benign (defaults, bounded, no crash); fix by threading `generate_if_absent:false` into `materialize_for_load` from `re_materialize_config`. Note later needs as TODOs and stop. **Slices 6.11–6.17 complete — player commands, chat, and the base-plugin suite (6 plugins), all merged + live-validated.** 6.11 — **players reach plugin commands both channels:** console commands via an `ISource2GameClients::ClientCommand` SourceHook (NOT ConCommand+FCVAR — CS2 doesn't forward unknown console commands; NOT an ExecuteStringCommand detour — its protobuf arg crashes on the wrong subobject); chat commands (`!`/`/`) via a **Host_Say detour** (CS2 fires no usable player_chat event) → `dispatch_chat`. **Colored chat SOLVED** — broadcast via the game's `UTIL_ClientPrintAll` (SM's PrintToChatAll), per-recipient via raw **SayText2 with `chat=false`** (a server message → respects inline color codes, NOT team-colored; a leading color byte is swallowed so prefix a space); `ChatColors` (from CSSharp's IL, live rainbow-verified) in `@s2script/cs2`. 6.13-A — **sender-aware `sm_say`** = a `FormatActivitySource` port (show-activity flags; non-admins see `ADMIN`, admins the real name), per-recipient colored. 6.13-B — **basechat as its own distributable plugin:** `@s2script/basechat` (say/chat/psay + the SM `@` / `@@` / `@`-team chat triggers) over a NEW engine-generic **`Chat.onMessage` raw-chat subscriber** — a notify-mux that threads `teamonly` through Host_Say→`dispatch_chat`, delivers non-command chat to subscribers, and suppresses the broadcast when one returns `HookResult >= Handled` (the write-direction for chat, at parity with OnGameFrame; validated GENERAL — antiflood is a non-command consumer); shared **`Activity.formatSource`** promoted into `@s2script/cs2` (dual-mode pure logic + node:test). **Also: a top-level `plugins/` distribution dir** (base plugins moved OUT of `examples/`; the layout call is made) + per-plugin **`tsconfig.json`** extending a root **`tsconfig.base.json`** that MIRRORS the `s2script build` typecheck options → editor IntelliSense resolves `@s2script/*` (build unaffected — the CLI builds its own in-memory program; the SvelteKit-internals pattern, static since the `packages/*` glob auto-covers). 6.14 — **`@s2script/playercommands`** (`sm_slap` moved from basecommands + `sm_slay` + `sm_rename`): `sm_slay` = **`CBasePlayerPawn::CommitSuicide` called by a SELF-RESOLVED byte SIGNATURE, never a vtable index** — on our build it's vtable index **819**, NOT ModSharp's 400 (a 419-slot drift that vindicates the "self-resolve, never borrow" doctrine + explains the failed `slice-6.8`); located via a schema-backed **`m_fNextSuicideTime` fingerprint** (the strings are downstream death-notice loggers, not the fn); a new engine-generic `pawn_commit_suicide` op serial-gates + `.text`-guards + calls `(pawn,false,true)`; a raw `health=0` write does NOT kill. `sm_rename` = a new engine-generic **`ent_write_string`** primitive (bounded memcpy+NUL, mirrors `read_string`, CORE-only) writing `m_iszPlayerName` (offset live-resolved) + `notifyStateChanged` — **propagation LIVE-CONFIRMED** (scoreboard updates). GOTCHA: `console.log` inside `pawn.js`'s RAW realm silently no-ops (console is injected per-plugin, not the raw prelude). 6.15 — **`@s2script/antiflood`** (first NON-command plugin): a passive chat-flood moderator over `Chat.onMessage` running a pure **leaky-bucket** model (tokens leak with elapsed time + cap when blocking → recovers by pausing ~1s) with a throttled RED slow-down notice; config-driven (`flood_time`/`max_tokens`, live-reload via `config.onChange`); throttles everyone (base SM — admin-immunity deferred). 6.16 — **`@s2script/adminhelp`** (`sm_help`, paginated flag-aware command list): a core **`__s2_commands_list`** native (mirrors `__s2_plugins_list`) + a `COMMAND_META` name→flags map — `__s2_concommand` gained an optional flags arg (register→0 / registerServer→-1 / registerAdmin→mask); `Commands.list()`. 6.17 — **`@s2script/basecomm`** (comms control): **`sm_gag`/`sm_ungag` VERIFIED** (suppress a gagged speaker's say/say_team via `Chat.onMessage`→Handled, **SteamID-keyed**); **`sm_mute`/`sm_silence` BEST-EFFORT** — the `m_bHasCommunicationAbuseMute` schema-write is proven but the voice-suppression EFFECT is UNVERIFIED (single-human gate; may be matchmaking/GC-driven) — the robust voice mute is a **`CServerSideClient::CLCMsg_VoiceData` detour** (libengine2, like the 6.6 damage hook), DEFERRED. **The base suite now ships 6 plugins: basecommands · basechat · playercommands · antiflood · adminhelp · basecomm.** Deferred (do NOT build ahead): the voice-mute CLCMsg_VoiceData detour (+2-human verify); admin-immunity system; `sm_slay` reply-gating on op-status; distribution via **npm workspaces + declared deps** (plugins currently resolve `@s2script/*` via the CLI's `packagesDir` + the editor tsconfigs, NOT node_modules). REMAINING SM defaults: basebans, funcommands, basetriggers, nextmap, adminmenu, basevotes, clientprefs, reservedslots, admin-flatfile. **Slice 6.18 complete — `basebans` (SteamID banning + connect-time enforcement; merged 95cb677 + pushed).** The 7th base plugin + the first CONNECT-TIME gate. `sm_ban <target> <min> [reason]` / `sm_unban <steamid>` / `sm_addban <steamid> <min> [reason]` over: (a) a **host-global `BAN_CACHE`** thread_local in core (SteamID64→(until,reason); MUST be core — its reader is the C++ connect hook, not a JS context — exactly the admin-cache rationale) + `__s2_ban_*` natives + a **shim→core FFI export `s2script_core_ban_check(xuid,now,out_reason,cap)`** (NOT a new `S2EngineOps` op; **fail-open** `catch_unwind`→0 so a core bug never wedges ALL connections) + an engine-generic **`@s2script/bans`** prelude (`Bans.add/remove/get/list/reload`, `until=Date.now()/1000+min*60`, 0=perm) persisting `bans.json` via the config bridge (`__s2_config_read_raw/write_raw("bans")` — the id is arbitrary, reused from admins.json); (b) **connect enforcement = a `ClientConnect` SourceHook** — `SH_DECL_HOOK6(ISource2GameClients, ClientConnect, …, bool, CPlayerSlot, const char* name, uint64 xuid, const char* netid, bool, CBufferString*)`, a **sibling `SH_ADD_HOOK` on the same `Source2GameClients001`** already hooked for `ClientCommand` (vtable layout already validated live) — the hook calls `ban_check(xuid, time(NULL))` and on a hit `RETURN_META_VALUE(MRES_SUPERCEDE, false)` → the engine rejects with **`NETWORK_DISCONNECT_REJECTED_BY_GAME`**; `xuid` (arg#3) is the **REAL decimal SteamID64, VALID at connect** (the Steam-auth-timing worry that it might be 0/partial pre-auth was DISPROVEN live). Boundary held (BAN_CACHE + ban_check are engine-generic core; `ISource2GameClients`/the connect hook are shim-only; both gates green). One sniper rebuild. `@s2script/basebans` (CS2) applies the NO_MULTI guard on `sm_ban` (refuse >1) + a **digit-regex `<minutes>` guard** (a typo'd/non-numeric minutes would else `argInt`→0 = a silent PERMANENT ban — a Task-2-review fix). **The full loop needs a HUMAN client** (`ClientConnect` isn't called for bots — same ceiling as SayText2/damage) — proven live on de_dust2 with a real client (`gkh` / `76561198006662865`): `[s2script] connect: … xuid=76561198006662865` (hook fires + real xuid), `sm_ban #2 1` → kicked (`NETWORK_DISCONNECT_KICKED`) + `bans.json` persists `{until:<unix>,reason}`, **reconnect while banned → `[s2script] rejected banned player gkh` + `NETWORK_DISCONNECT_REJECTED_BY_GAME`** (the definitive enforcement proof — `MRES_SUPERCEDE,false` reads the same `BAN_CACHE` `sm_ban` wrote), `sm_unban` → `bans.json` `{}` → reconnect → `SIGNONSTATE_FULL`; `RestartCount=0`, server ticking throughout. **Custom reject MESSAGE deferred** (filling `CBufferString*` calls tier1 `CBufferString::Insert` → the 5D.1 dlopen cascade; MVP = generic engine reject + a server-side reason log). **`sm_banip` deferred** (no IP at `ClientConnect`; needs a `client_address` op + engine `addip`/`writeip`). Known cosmetic Minor: the `_help` key drops from `bans.json` on the first rewrite (rebuilt from `BAN_CACHE`) — stays valid JSON. See `docs/superpowers/specs/2026-07-05-slice-6.18-basebans-design.md` + [[cs2-connect-hook-and-bans]]. **The ClientConnect gate is REUSABLE for `reservedslots`** (same connect hook, admit/reject on a reserved-slot check). **7 base plugins now ship: basecommands · basechat · playercommands · antiflood · adminhelp · basecomm · basebans.** REMAINING SM defaults: funcommands, basetriggers, nextmap, adminmenu, basevotes, clientprefs, reservedslots, admin-flatfile. **NEW THREAD — the ban-reason feature (user-driven): a banned player should SEE their ban reason, but CS2's disconnect protobuf has no reason field → the reject must ADMIT → show the reason → kick, and the ban DECISION must be JS-hookable (3rd-party ban systems query their own DB async and respond). Decomposed into 3 sub-projects: (1) a `Client` object + lifecycle events [DONE]; (2) console-print primitive + `Client.kickWithReason` + an `ip`/`client_address` op; (3) refactor `@s2script/bans` to `onConnect`→check→`kickWithReason` + flip the shim `ClientConnect` to always-admit. Sub-project 1 complete — `@s2script/clients` (merged c3199fd, pushed):** a first-class **engine-generic `Client`** handle (slot-backed; identity IS the slot — no serial needed; a pure JS wrapper over the EXISTING `__s2_client_*` ops, NO new engine primitive: `.slot`/`isValid()`/`steamId`/`name`/`userId`/`signonState`/`isBot`(=`steamId==="0"`)/`kick`/`chat`; lives in the CORE prelude beside `__s2pkg_events`, engine-generic, distinct from CS2's `Player`=controller/EntityRef) + the full **6 notify lifecycle events** — `onConnect`(`OnClientConnected`, fires for bots)/`onPutInServer`/`onActive`/`onFullyConnect`/`onDisconnect`/`onSettingsChanged` + `Clients.fromSlot`/`all()`. Mechanism: six `void` SourceHooks on the already-held `ISource2GameClients* m_gameClients` (signatures verbatim from `eiface.h:567-599`, confirmed vs CSSharp's live SourceHook param-info; `ClientDisconnect`'s `ENetworkDisconnectionReason` needs a `enum …:int;` forward-decl — a protobuf int-enum, valid redeclaration since `eiface.h` co-includes the `:int` stub) → each forwards `("<name>", slot.Get())` to ONE name-keyed FFI export `s2script_core_dispatch_client_event(name, slot)` → `dispatch_client_event` (a faithful `dispatch_chat_message` mirror MINUS suppress/return: snapshot-release, `try_borrow_mut` re-entrancy, per-sub `is_live`+TryCatch) → `CLIENT_MUX` (`event_mux::EventMux` reuse; `remove_by_owner` on unload, reset on shutdown). Handlers may be async (fire-and-forget the Promise). **ADDITIVE — the 6.18 `ClientConnect` reject is byte-for-byte untouched; both boundary gates green; 151/151 core tests; one sniper rebuild.** Investigation-confirmed DEFERRALS: `onAuthorized`/postauth (CS2 has NO auth-complete callback — CSSharp/ModSharp POLL `IVEngineServer2::IsClientFullyAuthenticated` per-frame, a stateful `RunAuthChecks` loop, unneeded since 6.18 proved connect-time SteamID64 is authoritative) → its own follow-up; reject-capable `onPreConnect` → sub-project 3. Built via subagent-driven-dev (3 tasks: core+prelude+types / 6 shim hooks / clients-demo), all reviews clean. **Live: 6 hooks install, the 6.18 ban hook ALSO installs (additive), `Clients.all()`=2, `onSettingsChanged` fired `slot=0 Seal`/`slot=1 Squad` through the FULL dispatch path with correct `Client.name` — the mechanism is live-proven; the FRESH-connect sequence (`onConnect→onPutInServer→onActive→onFullyConnect`+order, `onDisconnect`) is a DEFERRED live test (needs a human client; same proven path).** See `docs/superpowers/specs/2026-07-06-client-object-lifecycle-events-design.md` + [[client-lifecycle-module]] + [[deferred-live-tests]]. **Sub-project 2 complete — `Client.print`/`ip`/`kickWithReason` (merged 75240a8, pushed):** the feasibility flipped the risk assessment — the console-print is NOT a usermessage RE, it's **`IVEngineServer2::ClientPrintf(slot, msg)`** (`eiface.h:238`, "Print szMsg to the client console"), already **proven live-safe in 6.1b**, on the held `s_pEngine`, with the bot-skip guard (`GetPlayerNetInfo != null`) already in production. Two ABI-appended engine ops (after `pawn_commit_suicide`, consistent across the C struct + Rust mirror + shim `ops.` + BOTH test op-structs): `client_console_print` (→ `ClientPrintf`, bot-skip guarded — a print to a null-netchannel fake client segfaults) + `client_address` (→ `GetPlayerNetInfo->GetAddress`, `static std::string` pattern, `""` for bots). Pure-JS on `Client`: `print(msg)` (→ `client_console_print`, `+"\n"`), `ip` getter (→ `client_address`, `":port"` stripped via indexOf), and **`kickWithReason(reason, delaySeconds=5)`** = a per-slot `__s2_pendingKicks` map + ONE lazily-wired persistent `onActive` (deliver `chat`+`print`) + `onDisconnect` (cleanup — a Task-2-review fix, else a reused slot false-kicks the next client) + `globalThis.__s2pkg_timers.delay` → `fromSlot`-guarded `kick`; **contract: call from `onConnect`** (the ban path). ADDITIVE — the 6.18 reject + sub-project-1 lifecycle untouched; 153/0 core tests; boundary + typecheck green; one sniper rebuild. Built via subagent-driven-dev (Task 1 ops + Task 2 JS + a review-fix), all reviews clean, final review READY-TO-MERGE. **Live (bots-provable): the new prelude LOADS (`clients-demo onLoad — all()=2`), sub-project-1 events still fire, 6.18 bans unaffected, no crash. The real console line / real `.ip` / a full live `kickWithReason` need a human client → deferred live test.** ROBUSTNESS FOLLOW-UP for sub-project 3: the delayed kick re-resolves by SLOT at fire time — add a userId/steamId re-check so a same-slot reconnect inside the 5s delay isn't falsely kicked. See `docs/superpowers/specs/2026-07-06-client-console-kickwithreason-design.md` + [[client-lifecycle-module]] + [[deferred-live-tests]]. **NEXT: sub-project 3 — refactor `@s2script/bans` to `Clients.onConnect`→check `BAN_CACHE` (or a 3rd-party async source)→`Client.kickWithReason(formatReason(ban))` + FLIP the shim `ClientConnect` to always-admit (retire the 6.18 instant-reject) so the banned player SEES the reason (admit→show-reason→kick). This CHANGES ban behavior — the payoff of the whole thread.** **Sub-project 3 complete — THE BAN-REASON FLIP (merged 119403f, pushed); the ban-reason thread is DONE.** The 6.18 shim `ClientConnect` instant-reject SourceHook is **removed entirely** (`SH_DECL`/`SH_ADD`/`SH_REMOVE` + `Hook_ClientConnect` body + the `m_clientConnectHookInstalled` flag + the orphaned `<ctime>`/`CBufferString` forward-decl) — the shim now **admits every client**, and `OnClientConnected` (sub-project 1) drives the JS `onConnect` event. `@s2script/basebans` registers the enforcement: `Clients.onConnect(c => { if (c.isBot) return; const b = Bans.get(c.steamId); if (!b || (b.until !== 0 && b.until <= now)) return; c.kickWithReason("[SM] You are banned: " + reason + " (" + expiry + ")"); })` — a banned player is **admitted → shown the reason (chat + console) → kicked ~5s later** (via sub-project 2's `kickWithReason`), NOT the 6.18 instant `NETWORK_DISCONNECT_REJECTED_BY_GAME`. `sm_ban` still kicks the ONLINE player immediately; the `onConnect` handler is the RECONNECT enforcement + the extension point where a **3rd-party ban system registers its OWN `onConnect`** (querying its async DB) — the whole reason the user wanted JS-driven. ADDITIVE to the store (`BAN_CACHE`/`Bans`/`bans.json`/the `sm_*` commands unchanged); `s2script_core_ban_check` retained as an available-but-unused synchronous primitive. One sniper rebuild (shim removal). Controller-authored (small coupled flip) + a thorough opus review (Spec ✅, surgical-removal + expiry-boolean verified, no Critical/Important; 3 review-minors [orphaned forward-decl + stale comments] cleaned up). **Live (bots-provable): the reject-hook log is GONE (`--since 60s` count 0), the 6 lifecycle hooks install, basebans logs `connect enforcement registered`, bots are NOT falsely kicked (isBot guard on steamId "0"), no crash.** **DEFERRED HUMAN-CLIENT LIVE TEST (HIGH-PRIORITY — see [[deferred-live-tests]]): `sm_ban <self> 1 test` → reconnect → ADMITTED then reason in chat+console then kicked ~5s (not instant-reject); `sm_unban` → clean; expired → in. ⚠️ THE KEY RISK: is `c.steamId` the REAL SteamID64 (not `"0"`) at `onConnect`? — `Client.steamId`=`GetClientXUID(slot)` (client-list), NOT the ClientConnect `xuid` arg 6.18 proved; if it reads `"0"` (client-list XUID not yet populated at `OnClientConnected`), the `isBot` guard swallows it → the ban SILENTLY doesn't enforce → MOVE the check to `onPutInServer` (still before `onActive`, so `kickWithReason` still delivers).** ROBUSTNESS FOLLOW-UP (from sub-proj-2 review): the delayed kick re-resolves by SLOT — add a userId re-check at fire time so a same-slot reconnect inside the 5s delay isn't falsely kicked. See `docs/superpowers/specs/2026-07-06-ban-reason-flip-design.md` + [[client-lifecycle-module]]. **The 3-sub-project ban-reason feature (Client handle + lifecycle events → console-print/kickWithReason/ip → the JS enforcement flip) is complete; only the human-client live verification remains.** **UPDATE (merged e80f909, ✅ LIVE-VERIFIED with a human client 2026-07-06): the whole ban-reason feature works.** Follow-up fix: `sm_ban` on an ONLINE player was a SILENT `player.kick` (no reason shown) — now `sm_ban` uses `Clients.fromSlot(slot).kickWithReason(banMessage(...))`, and **`kickWithReason` was generalized to deliver-NOW when the client is already in-game (`signonState >= 4`, the sm_ban case) and DEFER to `onActive` when still-connecting (the reconnect case)** — both send the reason once/sec in **chat AND console** over the delay then kick (a shared `banMessage()` unifies the wording). Live-verified BOTH paths: reconnect ("worked for a user joining") + initial sm_ban ("Yep this is working"). **The ⚠️ steamId-at-onConnect risk is RESOLVED IN PRACTICE** — the reconnect path working proves `c.steamId` is the real SteamID64 at `onConnect` (no `onPutInServer` move needed). **Design decisions this session (user-confirmed): KEEP the current architecture — `slot` stays the id (NOT renamed to `clientId`: SM's client is 1-based, our slot 0-based → the rename is an off-by-one footgun; the `Client` OBJECT carries the "client" familiarity), KEEP the `@s2script/clients` lifecycle events as the low-level primitive AND the game events (SM ships both — connection forwards + game events; not redundant), NO `fromUserId` added (YAGNI). DEFERRED (not now): make `Client` a userid-GATED safe handle like `EntityRef` (a stored slot-backed Client silently reads a reused slot's new occupant — a charter gap; gate via `client_userid(slot)` re-check → null on reuse); the kickWithReason slot-reuse robustness (userId re-check at kick fire time). See [[deferred-live-tests]] (ban-reason now ✅), [[client-lifecycle-module]].** **The base suite ships 7 plugins; the ban-reason feature is DONE + working. REMAINING SM defaults: funcommands, basetriggers, nextmap, adminmenu, basevotes, funvotes, clientprefs, reservedslots, admin-flatfile. **Slice complete — reservedslots + basetriggers + chat-ordering fixes (merged 43d7531; 9 base plugins now ship).** A **server-info op batch** — `server_max_clients`/`server_map_name`/`server_game_time` → `Server.maxPlayers`/`mapName`/`gameTime`, all **typed calls on the already-held `INetworkGameServer*`** (the same ptr the 5D.2 client-list dereferences: `GetMaxClients()` idx12 / `GetMapName()` idx25 / `GetGlobals()->curtime` idx7; vtable indices ModSharp-cross-validated; null-guarded incl. the game_time double-guard; live: `maxPlayers=12`). **`@s2script/reservedslots`** — `Clients.onActive` admit-then-kick: a non-reserved client over `maxPlayers - reserved_slots` is kicked, keeping `reserved_slots` open for `ADMFLAG.RESERVATION` admins (enforced at onActive since steamId is "0" until auth; config `reserved_slots` int default 0; misconfig `reserved>=max` guarded). **`@s2script/basetriggers`** — `Chat.onMessage` → `timeleft` (`mp_timelimit` cvar − `Server.gameTime`), `thetime` (`Date`), `currentmap` (`Server.mapName`); `nextmap` deferred (answers from the `nextlevel` cvar / "Pending"). **CHAT-ORDERING FIX (SM parity, verified against SM's actual `core/ChatTriggers.cpp`):** a synchronous reply/broadcast in the Host_Say PRE-hook lands BEFORE the player's chat line. SM: `!` (public) does NOT suppress the say (`return false` → the `!slap` line SHOWS), `/` (silent) suppresses (hidden), and SM runs the command in `OnSayCommand_Post` (after the broadcast) so the reply is after. OUR FIX: the framework `ctx.reply` defers a PLAYER's reply one frame (`nextFrame().then(Chat.toSlot)`; console/rcon callerSlot<0 stays immediate), and basetriggers defers its `Chat.toAll` a frame — so `!slap`'s reply and the trigger answer land AFTER the line. Per-reply defer covers reply-feedback commands; a full Host_Say POST-hook (defer the whole command like SM) is the follow-up for broadcast commands. Live-verified end-to-end (`!slap` reply now after the command line; basetriggers answers in order). Built via subagent-driven-dev (op batch + 2 plugins) + 2 controller-authored ordering fixes; all reviews clean, final review READY-TO-MERGE, 153/0. Deferred (minors): an in-isolate test for the player-slot deferred reply (a rename of `__s2pkg_timers`/`__s2pkg_chat` would silently break all player replies — live-gate-only now); SM `ReplyToCommand` parity (a player's console-typed command reply routes to their chat, not console); the full POST-hook ordering. See `docs/superpowers/specs/2026-07-06-reservedslots-basetriggers-design.md`. **9 base plugins ship: basecommands · basechat · playercommands · antiflood · adminhelp · basecomm · basebans · reservedslots · basetriggers. REMAINING SM defaults: funcommands, nextmap, adminmenu, basevotes, funvotes, clientprefs, admin-flatfile.**** **Slice funcommands complete — the SM funcommands plugin + two engine write-primitives (merged 2db7727, pushed).** `@s2script/funcommands` (CS2): `sm_gravity`/`sm_noclip`/`sm_freeze`/`sm_unfreeze`, all `registerAdmin(ADMFLAG.SLAY)`; a `forEachPawn` helper resolves the target and — SM parity — **defaults to the caller (`@me`) when an IN-GAME player runs a command with no target arg** (the server console, `callerSlot < 0`, still requires an explicit target → usage error). Effects are schema-field WRITES (no game-function RE): gravity = `pawn.gravityScale`+`actualGravityScale` (generated setters); noclip/freeze = **`pawn.moveType`** toggled (`MoveType_t`: NONE=0/WALK=2/NOCLIP=7). Two new engine-generic primitives: **(1) narrow-int entity WRITE kinds** — `write_i8/i16/u8/u16/u32` (`core/src/entity.rs`) + kind-dispatch arms in the existing generic `s2_ent_ref_write` native (`core/src/v8host.rs`), faithfully mirroring the read arms (kinds `I8=4/I16=5/U8=6/U16=7/U32=8`; widths/signs verified) → extends the serial-gated `EntityRef.write*` surface beyond I32/F32/BOOL; **NO new `S2EngineOps` op** (reuses the generic `__s2_ent_ref_write` via new kind constants). **(2) `pawn.moveType`** (`games/cs2/js/pawn.js` + `packages/cs2/index.d.ts`) — a uint8 accessor over `m_MoveType`; the setter writes BOTH `m_MoveType` and `m_nActualMoveType` and calls `notifyStateChanged` (CS2 field names stay in the game layer). Boundary held (entity byte-writes are Source2-generic → core; both gates green); one sniper rebuild (the write arms). Final opus review READY-TO-MERGE (no Critical/Important; the one Minor — `notifyStateChanged` fires only for `m_MoveType`'s offset, not `m_nActualMoveType` — is documented + non-blocking, matches the existing `setVelocity` single-notify pattern, and the effect is live-confirmed). **`sm_blind` DEFERRED** — the flashbang fields (`flashDuration`/`flashMaxAlpha`) produced NO visible effect; a real blackout needs a **`CUserMessageFade`** user message via the SayText2 protobuf-reflection path (a new `client_fade` op) — documented follow-up. `sm_burn` (ignite game-fn, no framework sig) + `sm_beacon` (particle/temp-entity subsystem) also deferred. Proven live on de_dust2 post-update (`bot_quota 2`, rcon): `sm_gravity Sox 0.3`→"Set gravity for 1 player", `sm_noclip Sox`→"Toggled noclip for 1 player" (validates the new uint8 write on live `m_MoveType`), `sm_freeze Sox 2`→"Froze 1 player", `sm_noclip` (no arg from console)→"Usage: sm_noclip <target>" (self-default correctly excludes the console), no crash. **The in-game self-default (@me) is a human-client verify (bots can't type) — deferred live test.** **TREADMILL EVENT (first real CS2 update survived):** a Valve game update landed MID-SLICE → the container re-downloaded **~68 GB** (blocks boot until done) → the re-download **WIPED `gameinfo.gi`** (the Metamod SearchPath), so Metamod→shim→s2script silently didn't load (map loaded fine, no `[s2script]`/`[META]` lines) → FIX: re-run **`docker exec s2script-cs2 /patch-gameinfo.sh`** (idempotent; re-injects `Game csgo/addons/metamod`) + `docker compose restart cs2`. After re-patch: **`GAMEDATA VALIDATION: 11 ok, 0 FAILED`** — every signature (`GameEventManager` ctor-body-xref, `CommitSuicide`, the `DispatchTraceAttack` detour self-test) re-resolved CLEAN against the fresh binary — AND schema offsets **live-resolved with zero code change** (`sm_gravity`/`noclip`/`freeze` all correct). First real-world proof of the "green-within-48h-of-every-patch" treadmill thesis: the only manual step a game update forces is the one-line `gameinfo.gi` re-patch. **Test-hygiene fix (bundled, separate commit):** a PRE-EXISTING systemic flake — core `v8host::frame_tests` share three process-global capture buffers (`LOG`/`EV_SUBSCRIBED`/`EV_UNSUBSCRIBED`, test-only `Mutex<Vec<String>>`, each `.clear()`d at entry) → the tests were WRITTEN for SERIAL execution (the V8 `HOST` isolate + `ENGINE_OPS` are `thread_local!`/parallel-safe, but those shared buffers aren't); under parallel `cargo test` one test's `.clear()`/push corrupts a concurrent test's assertion (reproduces on `main`, tracks thread scheduling not any change — funcommands's added tests merely perturbed scheduling to surface it). Fixed via **`.cargo/config.toml` `[env] RUST_TEST_THREADS = "1"`** (force libtest single-threaded; full core suite ~0.4s serial) — verified green across 12 consecutive runs (was ~1-in-3 parallel failures). **10 base plugins ship: basecommands · basechat · playercommands · antiflood · adminhelp · basecomm · basebans · reservedslots · basetriggers · funcommands. REMAINING SM defaults: nextmap, adminmenu, basevotes, funvotes, clientprefs, admin-flatfile.** See `docs/superpowers/specs/2026-07-06-funcommands-design.md`.** **Slice SQLite-DB complete — the persistence primitive (merged 24128ac, pushed; implemented via a 5-task WORKFLOW).** An engine-generic async-API SQLite database — **`@s2script/db`** (`Database.open(name?)`/`query`/`execute`/`close`, all Promise-returning; `SqlValue=string|number|boolean|null`, parameterized `?`-only) over **`core/src/db.rs`** (rusqlite `bundled` — vendors SQLite's C, zero system deps) — the first new I/O primitive in s2script's **extension-model** surface (see [[extension-model-and-db-primitive]]). **Driver seam (the extensions dogfood):** `Database` depends only on a `Driver`/`DriverConnection` interface; the built-in SQLite driver IS a `Driver` (thin wrapper over the `__s2_sqlite_*` natives), and `registerDriver` lets a future MySQL/Postgres driver slot in — this slice stubs every connection name → local SQLite, deferring the `databases.cfg` remap + the remote driver (a TS driver over a future `@s2script/net` socket, OR a core-Rust driver). **KEY simplification (spec-noted):** execution is **synchronous behind the Promise API** this slice (the natives run rusqlite inline + resolve/reject the Promise — avoids surgery on the generic async resolver, which carries no payload); the API stays Promise-based so the threadpool move is a no-API-change follow-up (local SQLite is sub-ms). **Engine op:** `db_data_dir` (ABI-appended last; the shim resolves `<addon>/data` via the same `dladdr` walk as `ConfigPath`); core composes `<data>/<name>.sqlite` (name sanitized in core → no path traversal). **Safety:** a connection is an **opaque integer handle** (never a raw pointer to JS); it is **owner-scoped** — the registry stores `(Connection, owner_plugin)` and query/execute/close verify `current_plugin` owns the handle (a wrong owner reads as "invalid db handle", not probeable — the final-review finding #1, since a guessable integer handle usable cross-plugin is the "raw cross-plugin reference" the charter forbids); **ledgered** (`Resource::DbConn` → closed on plugin teardown even if never `close()`d); **degrade-never-crash** (`catch_unwind` on every native; a SQL/handle error rejects the Promise). Built via a **Workflow** (5 sequential implement→adversarial-review→fix tasks, 10 agents, 0 errors) then an opus final review (READY-TO-MERGE; finding #1 owner-scoping + 2 minors fixed post-review). **Deploy:** `data/` needs a nested RW mount (like `configs/`) — the addon is `:ro`; a live-gate finding (adding the volume needs a container recreate, which re-runs the entrypoint but LEFT gameinfo.gi patched this time). Proven live on de_dust2 (bots): `db-demo` opens a DB, CREATE+INSERT+SELECT round-trips, and **the boot-count PERSISTS across a restart** (`id 1→2`, autoincrement climbs — real on-disk persistence in the CS2 process), gamedata 11/0, no crash. **167 core tests** (full-public-API round-trip + registerDriver-seam + owner-scoping, all in-isolate). **DEFERRED follow-ons (do NOT build ahead): the MySQL/remote driver (+ the `@s2script/net` socket primitive); the `databases.cfg` named-connection remap; the threadpool (truly-non-blocking) execution move; the clientprefs plugin (cookie registry + SteamID keying + connect/disconnect caching + `sm_settings`/`sm_cookies` — also needs the deferred menu primitive); migrating admins.json/bans.json onto the DB; the extension-model design doc (mapping every SM-extension category → the s2script layer). See `docs/superpowers/specs/2026-07-06-sqlite-db-primitive-design.md` + `docs/superpowers/plans/2026-07-06-sqlite-db-primitive.md`.** **Slice clientprefs complete — SM-parity client-preference cookies, the DB primitive's first consumer (merged 15b3b38, pushed; implemented via a 5-task WORKFLOW).** SourceMod's clientprefs = a native EXTENSION (cache + DB + natives) + a separate menu PLUGIN; s2script mirrors the split. **Core** — a host-global cookie cache (`core/src/cookies.rs`: `steamid→{name→(value,dirty)}` + a `cached` flag; pure, unit-tested; **mirrors the admin/ban caches → NO shim change, NO engine op**) + 7 `set_native`'d natives (`__s2_cookie_get/set/load/get_dirty[→JS object]/clear/mark_cached/is_cached`; `catch_unwind` each). **`@s2script/clientprefs`** (engine-generic module, prelude runtime) — `Cookies.register`(idempotent)/`get`(bot-skip + default fallback)/`set`(dirty)/`areCached` + `CookieAccess` enum. **The `clientprefs` base plugin** — owns the DB lifecycle in JS (over `@s2script/db` + `@s2script/clients`): `CREATE TABLE cookies (steamid,name,value,updated, PK(steamid,name))`; **load** on `Clients.onPutInServer` (SteamID-keyed `db.query` → `__s2_cookie_load` → `mark_cached`; SM's `OnClientAuthorized` analog) + **dirty-flush** on `Clients.onDisconnect` (`get_dirty` → clear → `INSERT OR REPLACE`; **capture-then-clear** so the async writes use captured values). Bots (`steamId "0"`) skipped everywhere; dirty-tracked (SM parity). **`onCached` DEFERRED** — it's a cross-context event whose trigger (the plugin's async load) runs UNDER the isolate borrow, so a JS fan-out hits the `try_borrow_mut` re-entrancy skip (the `Events.fire`-can't-re-dispatch limit); firing it reliably needs a post-drain pending-dispatch (a follow-up, no consumer yet — gate on `areCached` for now). Built via a Workflow (5 sequential implement→review→fix, 10 agents, 0 errors) + an opus final review (READY-TO-MERGE; Minor 1 — `shutdown()` didn't reset the cookie cache like admin/ban → stale on a Metamod re-init — FIXED [`cookies::reset()`]; Minors 2 [module↔plugin both named `@s2script/clientprefs` — non-breaking, pre-registry] + 3 [an explicitly-set `""` reads back as the default — the `""`-is-both-miss-and-value edge] DEFERRED). Needed a sniper rebuild (core natives + prelude). Proven live on de_dust2 (bots): `[clientprefs] table ready, lifecycle hooked` + `[clientprefs-demo] demo_boots=1`→ restart →`demo_boots=2` (a cookie PERSISTS across restart through the FULL stack — register→get→set→dirty-flush→DB→reload→get — for a synthetic SteamID, since bots have no cookies), gamedata 11/0, no crash. **174 core tests.** **DEFERRED (do NOT build ahead): `onCached` (post-drain dispatch); the whole MENU surface (`SetCookiePrefabMenu`/`ShowCookieMenu`/`sm_settings`/`sm_cookies` — needs the menu primitive, SM keeps it a separate plugin too); `SetAuthIdCookie` (offline writes); `GetClientCookieTime`; access-level ENFORCEMENT (`CookieAccess` is stored, not enforced); fast-reconnect pending-op tracking; SM's two-table dbId normalization; the module↔plugin name-collision rename; the empty-string-vs-miss distinction; the real-client auto-lifecycle live test (a human-client deferred test — bots have no cookies). See `docs/superpowers/specs/2026-07-07-clientprefs-design.md` + `docs/superpowers/plans/2026-07-07-clientprefs.md` + [[extension-model-and-db-primitive]].** **Slice clientprefs-completion complete — the bounded clientprefs deferrals closed to SM-parity (merged 5c25f00, pushed; 4-task WORKFLOW).** User chose "complete clientprefs, skip YAGNI." (1) **Module RENAMED `@s2script/clientprefs` → `@s2script/cookies`** (the capability, matching entity/timers/db/clients; the base PLUGIN keeps `@s2script/clientprefs` = SM's plugin name; resolves the module↔plugin collision — review Minor 2 from the prior slice). (2) **Empty-string vs miss:** `cookies::get` → `Option<String>` (None=miss), the native returns `undefined` on miss, the module `v === undefined ? default : v` — a stored `""` now round-trips (was read as the default). (3) **`getTime`:** the cache `Entry` gains `updated:i64` (set/load/set_authid carry it — the core has no clock, the CALLER passes the timestamp); `Cookies.getTime`. (4) **`SetAuthIdCookie` (full offline, user-chosen):** `Cookies.setAuthId(steamId, cookie, value)` writes the cache (dirty) + pushes to a core `OFFLINE` queue; the `clientprefs` plugin drains it each **`OnGameFrame`** (idle-cheap empty check) → `INSERT OR REPLACE` — the only path that persists a write for a DISCONNECTED SteamID (the module can't touch the DB). (5) **`onCached` (the deferred SM `OnClientCookiesCached`):** a `COOKIE_CACHED_MUX` (event_mux) + a `COOKIE_CACHED_PENDING: Vec<i32>` — the plugin calls `__s2_cookie_dispatch_cached(slot)` after a load (runs UNDER the drain borrow → only a Vec push, no HOST); **`dispatch_pending_cookie_cached()` fans out AFTER `frame_async_drain()` in `ffi.rs` (HOST FREE → dodges the `try_borrow_mut` re-entrancy limit)**, mirroring `dispatch_client_event` verbatim. The module wrapper GUARDS `if (c) h(c)` (fromSlot is null if the client left in the load→fan-out window — review Minor 1, fixed). **SKIPPED (YAGNI, user-confirmed):** access-level enforcement (no menu surface), fast-reconnect pending-op tracking (~15ms race), SM's two-table dbId normalization. **No shim change / no engine op** (natives `set_native`'d; one `ffi.rs` call). Built via a Workflow (4 sequential implement→review→fix, 8 agents, 0 errors) + an opus final review (READY-TO-MERGE; 3 Minors [null-Client guard + 2 doc/stale] all fixed). Sniper rebuild. Proven live on de_dust2 (bots, synthetic SteamIDs): `demo_boots`1→2 (basic), **`authid_boots`1→2 (OFFLINE setAuthId — a SteamID the demo never flushes, persisted ONLY by the plugin's OnGameFrame drain)**, `empty=[]` (stored `""` reads `""`, not the default), `getTime`=real climbing unix-ts; server ticking, no crash. **182 core tests.** `onCached`'s real fire = a deferred human-client test (mechanism in-isolate-proven). **DEFERRED (do NOT build ahead): the MENU (`sm_settings`/`sm_cookies` — needs the menu primitive; unblocks adminmenu + basevotes too); access enforcement; the YAGNI three. See `docs/superpowers/specs/2026-07-07-clientprefs-completion-design.md`.** **Slice fetch/HTTP complete — the async HTTP primitive + the async-network-I/O foundation (merged eec4538, pushed; 4-task WORKFLOW). THE BIGGEST ENGINE ADDITION SINCE THE DB.** A web-parity **`fetch(url, opts) → Promise<Response>`** (`@s2script/http`) that runs fully OFF the game thread. **The concurrency insight (user-driven):** it's not one fetch per client — it's `players × plugins-that-callout-on-connect × their patterns`, operator-uncontrollable, latency-critical (permissions on the join path). So a thread-per-request/blocking pool is the WRONG model; the answer is **async I/O**: an internal **`tokio` multi-threaded runtime** (`core/src/http.rs`, ~4 background threads, process-global `OnceLock` like the async_rt POOL) + a shared **`reqwest::Client`** (rustls/**ring** TLS, connection pooling/keep-alive) multiplexing thousands of concurrent requests over few threads via epoll. **NOT the Node/Deno objection** — tokio is an INTERNAL engine on background threads (invisible to plugins); the main thread only does the instant `fetch()` hand-off + the resolve-on-drain. **The async-result spine (the reusable bit the DB slice deferred):** `__s2_fetch` mirrors `s2_thread_sleep` (RESOLVERS + record_job + PENDING_JOBS + refresh_detour) but hands off to `http::fetch` (spawns onto the runtime, returns instantly — NO block); the tokio task runs the request + sends `(id, Result<FetchResponse,String>)` down an mpsc channel; a new frame-drain step (after the pool loop) drains `http::try_recv_completed()` + `resolve_fetch` builds the `Response` + resolves — mirroring `resolve_or_drop` (owner-context + liveness: a fetch outstanding when its plugin unloads DROPS, never resolves into a dead context). **API:** `fetch(url,{method,headers,body,timeoutMs})` → `Response{status,ok,statusText,headers,text(),json()}` (buffered body → sync text()/json(); UTF-8 text, binary deferred). **Error semantics (web-fetch parity, right for the join path):** an HTTP status (4xx/5xx) RESOLVES with `ok:false` (a 403 permissions answer is NOT a failure); only network errors / TIMEOUT (per-request, default 30s) REJECT. **Safety:** the body is **streamed with a running 10MB cap** (`resp.chunk()` loop, aborts past MAX_BODY — a review-fix; the naive `bytes()` let a chunked/no-Content-Length hostile response OOM the in-process server). SSRF/allowlist + `maxConcurrent` deferred to the permissions system. **No shim change / no S2EngineOps op** (tokio/reqwest in core like rusqlite; wiring = `http::init()` at core init + one drain step + the native). New deps: `tokio`(rt-multi-thread/net/time) + `reqwest`(rustls-tls → ring) — a sizeable tree, amortized across the async-network category. Built via a Workflow (4 sequential implement→review→fix, 8 agents, 0 errors) + an opus final review (READY-TO-MERGE; every load-bearing invariant verified — main-never-blocks, liveness/UAF, drain-balance, id-reuse, re-entrancy; Minor 1 body-cap streaming FIXED, Minor 2 uninitialized-engine leak = unreachable, deferred). One sniper rebuild (compiles tokio+reqwest — the slow one; verified builds locally + in bullseye). **Proven live on de_dust2 (bots-provable):** `[http-demo] firing 10 concurrent fetches (frames=0)` → **`10/10 ok; tick advanced 46 frames during the fetches; single=200 len=220`** — 10 concurrent HTTPS requests to a real public API (httpbin.org) all resolved 200 (TLS + concurrency + the full marshalling), AND **the game frame counter climbed ~46 frames WHILE they were in flight = the decisive proof the main thread NEVER blocked** (a blocking fetch would show ~0). RestartCount=0, no crash. **190 core tests.** **FOUNDATION for the async-network category:** the tokio runtime + the cross-context event-delivery spine (onCached/client-events) make **websockets** (`tokio-tungstenite` + `WebSocket` handle + `onMessage`/`onClose` over the event spine) and **raw `@s2script/net` TCP/UDP** (`tokio::net`) NAMED FOLLOW-ONS — each mostly "add the protocol + wire the events." **DEFERRED (do NOT build ahead): websockets; raw @s2script/net sockets; binary request/response bodies (ArrayBuffer); per-plugin maxConcurrent/politeness; SSRF allowlist / a permissions gate; a true streaming-cap unit test (>10MB). See `docs/superpowers/specs/2026-07-07-fetch-http-design.md` + [[fetch-http-async-engine]].** **Slice WebSocket complete — the first real-time capability, the fetch foundation's SECOND consumer + the proof it COMPOSES (merged 6664365, pushed; 4-task WORKFLOW).** An engine-generic client **`WebSocket.connect(url) → Promise<WebSocket>`** (`@s2script/ws`) with `send`(text)/`close` + additive `onMessage`/`onClose`/`onError`, fully OFF the game thread. **The compose insight (why this was cheap):** a WebSocket is literally **fetch's one-shot async-result resolver (for the open handshake) + `onCached`'s post-drain cross-context event mux (for the message stream)** — the two hard pieces already existed; only the protocol (`tokio-tungstenite`, rustls/**ring**, `wss://`) + the per-connection glue are new. **Mechanism (`core/src/ws.rs`, NO shim change / NO S2EngineOps op — like reqwest, `tokio-tungstenite` lives in core on the SHARED `http.rs` runtime via a new `http::spawn` accessor):** per connection a tokio task `connect_async`es then `select!`s reading incoming frames ↔ writing queued `WsCommand`s, emitting everything as a `WsSignal{conn_id, kind: Connected|ConnectFailed|Message|Closed|Errored}` down one mpsc the frame drain polls; a registry `conns: id→{cmd_tx, owner}`. **The drain routes each signal to one of the two existing spines:** `Connected`/`ConnectFailed` → **resolve/reject the connect Promise** (reuses `resolve_fetch`/`RESOLVERS`/`record_job`/`PENDING_JOBS`; `resolve_ws_connect` resolves with the conn-id Number); `Message`/`Closed`/`Errored` → **queue to `WS_EVENT_PENDING`, fan out via `dispatch_pending_ws_events` AFTER `frame_async_drain()` (HOST free)** carrying the payload (mirrors `dispatch_pending_cookie_cached` verbatim — snapshot/`try_borrow_mut`/`is_live`/TryCatch). **Early-message ordering is gap-free by DRAIN ORDER, not a buffer:** within a frame — (1) `Connected` resolves the Promise, (2) the microtask checkpoint runs the `.then` where the plugin subscribes `onMessage` SYNCHRONOUSLY, (3) `Message` fan-out runs post-checkpoint — so a message never reaches an unsubscribed conn IF the plugin subscribes synchronously after `await connect` (the documented pattern; an `await` BETWEEN connect and onMessage drops in-gap messages — a per-conn buffer is the named follow-up). **Safety:** owner-scoped opaque integer conn ids (`send`/`close`/`on` verify `current_plugin` owns it — a guessable id can't touch another plugin's socket; a review-CRITICAL fix added `is_owner` gating on `__s2_ws_on`); ledgered `Resource::WsConn` → teardown `drop_conn`+`remove_by_owner` before context disposal; a signal for an unloaded conn finds no resolver/live sub → dropped. **Close vs Shutdown split (a review fix):** JS `ws.close()` → `WsCommand::Close` emits `Closed(1000)` (onClose fires + cleanup); ledger-teardown → `WsCommand::Shutdown` emits NO signal (conn ids are monotonic + the registry entry is removed synchronously → no stale `Closed` can misroute into a reused id). **Final-review finding (Important, FIXED in-slice, 6664365):** `WS_EVENT_MUX` keys for a closed conn were never pruned → a reconnect-on-close loop (fresh monotonic ids) accumulated 3 dead mux entries + 3 retained JS closure Globals per cycle (bounded only at unload). Fix: a new `EventMux::remove_by_name` (drop a whole key regardless of owner) + `dispatch_pending_ws_events` prunes the conn's message/close/error keys AFTER the terminal `"close"` fan-out (outside the empty-sub check, so an onMessage-only conn is still pruned); the read-error arm now emits `Errored` THEN `Closed(1006)` (browser parity: error→close) so EVERY teardown path (peer close, self-close, stream-end, read-error) funnels through ONE `"close"` prune site + `drop_conn`; `ws::close` returns the real send outcome. Built via a Workflow (4 sequential implement→adversarial-review→fix, 8 agents, 0 errors) + an opus final review (READY-TO-MERGE; every load-bearing invariant — non-blocking handoff, drain ordering, UAF/liveness, owner-scoping, Close/Shutdown split, PENDING_JOBS balance — verified). New core deps `tokio-tungstenite`(rustls-tls-webpki-roots) + `futures-util`; one sniper rebuild. **200 core tests.** Proven live on de_dust2 (bots-provable, public wss:// echo `ws.postman-echo.com`): `[ws-demo] connected + sent (frames=24)` → `echo=hello-from-s2script; tick advanced 30 frames while connecting/echoing` (the full connect→send→onMessage round-trip AND the tick NEVER stalled = non-blocking) → `closed code=1000` (clean self-close, onClose delivered the code, the terminal prune ran after), gamedata 11/0, RestartCount=0, no crash. **DEFERRED (do NOT build ahead): a WS SERVER (listen — a much larger separate thing); BINARY messages (text/UTF-8 MVP); backpressure/bounded channels; the per-conn early-message buffer; subprotocols; automatic reconnection (the plugin's job); per-message ack. NEXT async-network follow-on: raw `@s2script/net` TCP/UDP sockets (`tokio::net` + the same streaming pattern) — which then unblocks the deferred MySQL/Postgres DB driver. See `docs/superpowers/specs/2026-07-07-websocket-design.md` + [[fetch-http-async-engine]].** **Slice MENU complete — SM-parity interactive player menus (chat + WASD center backends), the biggest remaining base-plugin-parity gap — unblocks adminmenu/basevotes/funvotes/sm_settings (merged 432b704, pushed; built via a 5-task WORKFLOW then live-iterated with a human client).** **Engine-generic `@s2script/menu`** (model + pagination + `registerRenderer` seam + the CHAT renderer, in the core prelude, SLOT-based like `@s2script/commands` — never imports `@s2script/cs2`): `new Menu(title)` + `addItem(info, display, {disabled?})` + `style` (`MenuStyle.Chat|Center`) + `exitButton` + `freezePlayer` + `onSelect({slot,item,info,display})`/`onCancel({slot,reason})` + `display(slot, seconds)`/`close(slot)`. Auto pagination (7 items/page chat: keys 1-7 + 8=Back/9=Next/0=Exit; center: a scrolling window). One active menu per player **per plugin** (context-local; cross-plugin coordination = the deferred global menu manager). Renderer seam mirrors the DB `Driver` seam. **CS2 center renderer (`games/cs2/js/pawn.js`, registers `"center"` through the seam — the ONLY CS2 menu facts):** WASD input by **schema-polling the button mask** (`m_nButtons.m_pButtonStates` via the `m_pMovementServices` chain, `EntityRef.readUInt64Via` — NO new native, reuses 5C.5) on a lazy `OnGameFrame` poll, rising-edge W/S/E→moveUp/moveDown/confirm; **display via the `show_survival_respawn_status` game event's `loc_token` HTML field** re-sent each tick (short `MENU_TTL=1` so it self-clears when re-sending stops; the clear sends a non-empty space — an empty `loc_token` is ignored by the HUD). A bounded **HEADER/CONTENT/FOOTER** layout (CS2 center HTML is one flowing block → the content is a fixed-height 6-row scrolled window keeping the cursor on-screen; the footer — the WASD legend + page/scroll indicators — is always the last line, never pushed off; font tiers `fontSize-m`/`sm`/`s`, CS2MenuManager parity). `freezePlayer` freezes `moveType` to `MOVETYPE_NONE` while open + restores on close (buttons still register → nav still works). **ONE new engine-generic op — `event_fire_to_client(slot)` → `Events.fireToClient(slot, name, fields)`** (`@s2script/events`, SourceMod `FireToClient` parity): fires a game event to ONE client (not broadcast), reusing 5D.3's event create/set machinery + a **sig-scanned `GetLegacyGameEventListener(slot)` engine function** (a frameless leaf indexing a global per-client-listener array) → `IGameEventListener2::FireGameEvent` → serializes to that client's netchannel (bypasses `IGameEventManager2::FireEvent`, so NO pre-hook/re-entrancy). **RE finding (the workflow's guess was WRONG + would have crashed):** the per-client listener is NOT a `CServerSideClient` offset cast — CSSharp reaches it via the sig-resolved function; caught offline before any human opened a center menu. The `LegacyGameEventListener` DIRECT signature (from the CSSharp gamedata) **matched our pinned libserver.so** → `GAMEDATA VALIDATION: 12 ok, 0 FAILED`. Built via a Workflow (5 sequential implement→adversarial-review→fix, 18 agents, 0 errors) + an opus final review (READY-TO-MERGE; Important finding — center menus couldn't reach the Next/Back/Exit controls [`_navTargets` now drives the center cursor over items + controls] — + 2 minors, all fixed). **212 core tests.** Proven live on de_dust2 WITH A HUMAN CLIENT (the human-nav ceiling that SayText2/damage still defer): `sm_chatmenu` (numbered chat, pick a number → `onSelect` fires the right info → applies heal/noclip), `sm_menu` (center HTML render, W/S scrolls the window with a pinned footer, E selects/paginates/exits, `freezePlayer` locks movement while nav still works, prompt clear on select), RestartCount=0, no crash. Needed sniper rebuilds (the op + the `freezePlayer` prelude flag; the render/UX iteration was JS-only pawn.js redeploys). **DEFERRED (do NOT build ahead): a core global menu manager (cross-plugin menu coordination + SM's multi-menu stack/priority); a per-player style cookie (`sm_settings` "menu style" — plugin picks for now, clientprefs available); `VoteMenu`/`displayToAll` (the basevotes slice); per-item draw styles beyond enabled/disabled; a generic (non-CS2) center renderer; per-menu custom header/footer TEXT (a subtitle / footer note — the auto layout suffices for the base plugins). See `docs/superpowers/specs/2026-07-07-menu-primitive-design.md` + `docs/superpowers/plans/2026-07-07-menu-primitive.md` + [[cs2-menu-and-per-client-events]].** **Slice menu-primitive complete — SM-parity interactive menus (chat + WASD center backends); Task 5's sniper rebuild/deploy/live-gate (a blocking-review gap) now closed.** `@s2script/menu` (engine-generic, core prelude) ships `Menu`/`MenuStyle`/pagination/`onSelect`/`onCancel` + a `registerRenderer` seam + a chat renderer (numbered picks, timeout, disconnect-close, all composed off already-ledgered subs — no new teardown code) — plus a new engine-generic `event_fire_to_client` op/`Events.fireToClient` (SourceMod `FireToClient` parity; ABI-appended after `db_data_dir`; review-fixed a leak on the invalid-slot/bot-miss path + added an explicit `GetPlayerNetInfo` bot-skip guard) — plus the CS2-only `"center"` renderer in `pawn.js` (a lazily-armed `OnGameFrame` W/S/E button-mask poll via the existing `EntityRef.readUInt64Via` chain-read, no new native; renders the menu as `show_survival_respawn_status` loc_token HTML re-sent every tick — CS2 repaints that event's HTML for one frame only — including `userid` in the payload, a review-fix since CS2's per-client HUD handler filters on it). `menu-demo` (`sm_menu`/`sm_chatmenu`, both `MenuStyle` backends + 8-item forced pagination + a per-bot input-primitive liveness log). **The deferred half of Task 5** (sniper rebuild + deploy + live gate) is now done: a bullseye sniper rebuild (`libs2script_core.so` GLIBC≤2.30, `s2script.so` GLIBC≤2.14) + a full 16-plugin deploy to `dist/addons/s2script/plugins` + a `docker compose restart cs2` re-bind (the prior `package-addon.sh` `rm -rf` had left the container's addon mount stale/empty — a plain `restart`, not `--force-recreate`, re-binds it and leaves `gameinfo.gi` untouched) all pass. Proven live on de_inferno (`bot_quota 2`): `=== GAMEDATA VALIDATION: 12 ok, 0 FAILED ===`; `ConCommand 'sm_menu'/'sm_chatmenu' registered`; `[menu-demo] frame=N bot0 movementServices=live` repeating every ~2s across 4000+ frames (the WASD input primitive live on a bot pawn); `sm_menu`/`sm_chatmenu` invoked via rcon (console, `callerSlot<0`) correctly reply `"run in-game"` (the SM-parity console guard) with no crash; `RestartCount=0`. **DEFERRED (human-client only, mechanism proven, same ceiling as the SayText2/damage/basecomm-voice deferred tests):** the actual chat-menu number-pick, the center-menu W/S/E navigate-and-select, and visually confirming the HTML render — bots can't type chat or send real per-key button input. 208 core tests; both boundary gates green; `scripts/check-plugins-typecheck.sh` green across all 16 plugins. See `docs/superpowers/specs/2026-07-07-menu-primitive-design.md` + `docs/superpowers/plans/2026-07-07-menu-primitive.md`.**
