---
"@s2script/sdk": minor
---

Add TopMenu `sheets` and the `!menu` / `sm_menu` player hub.

Items default to the admin sheet. Plugins opt into the player hub with `sheets: ["menu"]`. `sm_admin` is unchanged; cookbook's demo command is now `sm_menudemo`.
