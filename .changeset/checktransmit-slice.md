---
"@s2script/sdk": minor
---

transmit: new builtin capability (`@s2script/sdk/transmit`) — per-client entity visibility filtering via a Source2 CheckTransmit post-hook. Declarative rules (`Transmit.setVisibleTo`/`reset`/`resetAll`/`stats`); multiple plugins AND-merge; zero JS in the per-snapshot hot path.
