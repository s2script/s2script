---
"@s2script/cs2": minor
---

Add next-frame coalesced `invalidate()` updates and per-view `lastUpdateResult()` for modal and dashboard components while preserving synchronous `refresh()` behavior.
