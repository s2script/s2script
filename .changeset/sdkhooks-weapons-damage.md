---
"@s2script/sdk": minor
---

SDKHooks remaining wiki types: `OnTakeDamagePost` (DTA after the original), Weapon*/Reload (VP, degrade without a signature), and `OnTakeDamageAlive` / `TraceAttack` / `FireBulletsPost` (typecheck, `SDKHook` returns `false` until a live path exists).
