---
"@s2script/sdk": minor
---

Accept `export function OnPluginStart` as a plugin artifact (alongside `plugin()`). `s2s create` scaffolds publics. Load-window `topmenu` and `translations` bind on `@s2script/sdk/plugin`.
