---
"@s2script/cs2": patch
---

Bind modal and dashboard clicks to copied rows, tabs, and footer actions from the last complete paint. Add optional stable modal row IDs and reject duplicate supplied IDs so handlers can safely revalidate current domain state.
