---
"@s2script/sdk": minor
---

Add ctx-free load-window APIs (`command` / `command.admin` / `command.server`, `hook.damage`, free `publish` / `use` / `tryUse`) bound to the plugin factory. Command handlers may return `HookResult`. `s2s build` treats producer-as-import specifiers as dependency usage.
