---
"@s2script/sdk": minor
---

First-party SDKHooks: `SDKHook` / `SDKUnhook` / `SDKHookType` (OnTakeDamage). The fake `OnTakeDamage` named public and `ctx.entities.onDamage` are removed. 0.x minor bump.
