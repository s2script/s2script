---
"@s2script/sdk": minor
---

Add provider-qualified `bindForwards(name, handlers)` maps with exact inferred payload and synchronous decision types, atomic registration rollback, and whole-map disposal.
