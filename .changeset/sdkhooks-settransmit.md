---
"@s2script/sdk": minor
---

SDKHookType.SetTransmit on the existing CheckTransmit POST path. AND-merge with Transmit.setVisibleTo: either API can hide; SetTransmit cannot un-hide a native mask clear. There is no SetTransmitPost.
