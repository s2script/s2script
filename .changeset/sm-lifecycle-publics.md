---
"@s2script/sdk": minor
---

Host-subscribe remaining SourceMod-named publics (`OnClient*`, `OnMapEnd`, `OnConfigsExecuted`, `OnAllPluginsLoaded`). `hook.event` / `hook.output` / `hook.topmenu` plus load-window `createScope`. `Command` is an alias for `CommandInvocation`; `config.onChange` is typed.
