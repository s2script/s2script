---
"@s2script/sdk": minor
---

Add opt-in protocol 2 contracts for synchronous plugin methods and typed notifications. The CLI derives wire schemas and verified interface-name associations, checks producer implementations, and includes canonical contract digests in archives. Protocol 2 requires host API 3; the new host also accepts existing API 2 protocol 1 archives.
