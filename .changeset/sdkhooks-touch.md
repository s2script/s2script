---
"@s2script/sdk": minor
---

SDKHooks Touch family (`StartTouch` / `Touch` / `EndTouch` / `Blocked` and `*Post`) on the existing `SDKHook` / `SDKUnhook` API. Wiki names with no engine backing return `false`; unknown type strings still throw.
