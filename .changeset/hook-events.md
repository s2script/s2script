---
"@s2script/sdk": minor
---

Game-event subscriptions are `hook.events.on` / `hook.events.onPre` (no positional `"pre"` phase). `hook.topmenu` is removed; use the free `topmenu` export. 0.x minor bump.
