---
"@s2script/sdk": minor
---

Add `s2s migrate engine-functions` to analyze v1 plugin gamedata and source references, report ambiguities, and atomically emit a v2 declaration only when the declared contract converts without loss.
