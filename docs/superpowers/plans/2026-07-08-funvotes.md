# funvotes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Ship the SM `funvotes` plugin — admin Yes/No votes that toggle a cvar or slay a player on pass.

**Architecture:** One CS2/game-layer plugin in `disabled/funvotes/` (opt-in). JS-only, no sniper. Composes `@s2script/votes`, `@s2script/server` (getCvar/setCvar), `@s2script/cs2` (Player.target/fromUserId/pawn.slay), `@s2script/commands`, `@s2script/admin`, `@s2script/chat`.

**Tech Stack:** TypeScript (pure ESM), `s2script build`.

## Global Constraints

- Ships opt-in in `disabled/funvotes/` (output `disabled/funvotes/dist/_s2script_funvotes.s2sp`).
- Pure ESM named imports; the 5E.1 typecheck gate (`module: ESNext`, strict) MUST pass.
- NO core/shim/op change, no sniper.
- Injection guard: the `sv_gravity` value must match `^[0-9]+(\.[0-9]+)?$` before it reaches `Server.setCvar`; toggle values are literal `"0"`/`"1"`.
- All 4 commands are `Commands.registerAdmin(name, ADMFLAG.VOTE, handler)`.
- `VoteResult.winner` is the index of the most-voted option (or null); **pass iff `winner === 0`** (Yes is options[0]).
- Config keys (exact): `funvote_duration` (int 20), `funvote_show_tally` (bool true).
- Commit with the trailer `Claude-Session: https://claude.ai/code/session_01G8RQTQTdmeczdE6oihp8Pn`; `git commit -F -` (no backticks).
- VERIFY every `@s2script/*` API against `packages/<mod>/index.d.ts`. Reference: `plugins/playercommands/src/plugin.ts` (Player.target + pawn.slay), `plugins/basevotes/src/plugin.ts` (Vote usage), `disabled/rockthevote/src/plugin.ts` (the config + Vote.start + VoteResult-index pattern).

---

### Task 1: The funvotes plugin (all 4 commands + the Yes/No helper)

**Files:**
- Create: `disabled/funvotes/package.json` (id `@s2script/funvotes`, the 2 config keys), `disabled/funvotes/tsconfig.json` (extends the root base, like `disabled/rockthevote/tsconfig.json`), `disabled/funvotes/src/plugin.ts`.

**Interfaces:**
- Consumes: `Commands.registerAdmin(name, flags, ctx => …)` + `ctx.arg(0)`/`ctx.callerSlot`/`ctx.reply`; `ADMFLAG.VOTE`; `Vote.start(cfg): boolean` + `Vote.isActive()` + `onEnd: (r: VoteResult) => void` (`VoteResult.winner: number | null`, an index); `Chat.toAll`; `Server.getCvar(name): string` + `Server.setCvar(name, value): void`; `Player.target(pattern, callerSlot): Player[]`, `Player.fromUserId(id): Player | null`, `player.userId`, `player.playerName`, `player.pawn` (→ `pawn.slay()`); `config.getInt`/`getBool`.
- Produces: no exports beyond `onLoad`/`onUnload`.

- [ ] **Step 1: Scaffold.** `package.json`/`tsconfig.json` modeled on `disabled/rockthevote/`. `s2script.config`: `funvote_duration` (int 20), `funvote_show_tally` (bool true). id `@s2script/funvotes`.

- [ ] **Step 2: `plugin.ts`.**
  - `startYesNo(reply: (m: string) => void, question: string, onPass: () => void): void`:
    ```
    if (Vote.isActive()) { reply("A vote is already running."); return; }
    Vote.start({ question, options: ["Yes", "No"], duration: config.getInt("funvote_duration"),
      showLiveTally: config.getBool("funvote_show_tally"),
      onEnd: (r) => { if (r.winner === 0) { Chat.toAll("[Vote] Passed: " + question); onPass(); }
                      else { Chat.toAll("[Vote] Failed: " + question); } } });
    reply("Vote started.");
    ```
  - `onLoad` registers (each `registerAdmin(name, ADMFLAG.VOTE, ctx => …)`):
    - `sm_votealltalk`: `const on = ["1","true"].includes(Server.getCvar("sv_alltalk")); startYesNo(ctx.reply, (on ? "Disable" : "Enable") + " AllTalk?", () => Server.setCvar("sv_alltalk", on ? "0" : "1"));`
    - `sm_voteff`: same against `mp_friendlyfire`.
    - `sm_votegravity`: `const v = ctx.arg(0); if (!/^[0-9]+(\.[0-9]+)?$/.test(v)) { ctx.reply("Usage: sm_votegravity <number>"); return; } startYesNo(ctx.reply, "Set gravity to " + v + "?", () => Server.setCvar("sv_gravity", v));`
    - `sm_voteslay`: `const targets = Player.target(ctx.arg(0), ctx.callerSlot); if (targets.length === 0) { ctx.reply("No matching players"); return; } if (targets.length > 1) { ctx.reply("Multiple players match — be specific"); return; } const uid = targets[0].userId; const name = targets[0].playerName ?? "player"; startYesNo(ctx.reply, "Slay " + name + "?", () => { const p = Player.fromUserId(uid); if (p && p.pawn) p.pawn.slay(); });`
    - a `console.log("[funvotes] onLoad — votealltalk/voteff/votegravity/voteslay registered")`.
  - `onUnload`: a log line.
  - NOTE: `ctx.reply` is a bound method — pass `(m) => ctx.reply(m)` or `ctx.reply.bind(ctx)` if a direct reference misbehaves; verify it compiles.

- [ ] **Step 3: Build + typecheck.** `node packages/cli/dist/cli.js build disabled/funvotes` MUST print the `.s2sp` path. Fix TS errors against the real `.d.ts`.

- [ ] **Step 4: Commit** `feat(funvotes): sm_votealltalk/voteff/votegravity/voteslay — admin Yes/No votes (disabled/)`.

## Self-Review

- Spec coverage: 4 commands + the Yes/No helper + pass-on-winner-0 + cvar-read-to-frame-question + slay-re-resolve-by-userId + the gravity injection guard + one-vote-per-context. All in Task 1.
- No placeholders: every handler body is spelled out above.
