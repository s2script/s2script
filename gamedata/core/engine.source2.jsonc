// DO NOT EDIT THIS FILE. It ships with s2script and an upgrade OVERWRITES it — any change
// you make here is lost on the next update. To fix or add an entry on a live server, drop a
// .jsonc file in gamedata/core/custom/ instead: it is applied last, replaces whole named
// entries, needs no rebuild, and survives upgrades. See docs/INSTALL.md ("Hot-fixing a
// broken signature").
//
// Owner: core (shim C++ / core Rust name every key here).
// Target: Source 2 engine — "engine": "source2". These are the engine-module interface
// strings that are the same on every Source 2 game; the CS2 server-module interfaces (and
// everything else CS2-specific) live in game.cs2.jsonc instead.
{
  "interfaces": {
    "SchemaSystem": "SchemaSystem_001",
    "EngineCvar": "VEngineCvar007",
    // IVEngineServer2 — backs client_print via ClientPrintf (Slice 6.1b; console-level).
    "EngineToServer": "Source2EngineToServer001",
    "NetworkServerService": "NetworkServerService_001",
    // INetworkMessages — used by client_print to FindNetworkMessage + AllocateMessage (Slice 6.1).
    // Interface string from inetworkmessages.h NETWORKMESSAGES_INTERFACE_VERSION macro.
    "NetworkMessages": "NetworkMessagesVersion001"
  }
}
