# @s2script/zones

## 0.2.0

### Minor Changes

- 82501e0: `@s2script/zones` interface additions: `Zone.tags`, `getZonesByTag(tag)`, `setZoneTags(name, tags)`, and the `created`/`deleted` zone-lifecycle events (`ZoneCreatedEvent`/`ZoneDeletedEvent`).

## 0.1.1

### Patch Changes

- 5fcc41f: Initial public npm release of the `@s2script/*` types packages and CLI (Changesets pipeline).
