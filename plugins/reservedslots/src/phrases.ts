/** reservedslots — English seed. Generated into translations/reservedslots.phrases.json by scripts/gen-phrases.mjs. */
export const phrases = {
  // Client.kick's reason argument is the ENGINE disconnect UI, not the chat/console funnel — it
  // never expands colour tags, so this keeps its "[SM] " prefix as literal text, no {tag}.
  "Kick Reserved Slot": "[SM] This server has reserved slots — you were disconnected to keep a slot open for a reserved player.",
};
