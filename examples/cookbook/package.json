{
  "name": "@example/cookbook",
  "version": "0.1.0",
  "main": "src/plugin.ts",
  "dependencies": {
    "@s2script/sdk": "^0.1.0",
    "@s2script/cs2": "^0.5.0"
  },
  "s2script": {
    "config": {
      "greeting": {
        "type": "string",
        "default": "Hello from the cookbook's config file!",
        "description": "Printed by cb_config (examples/cookbook/src/recipes/config.ts). Edit the materialized config file to change it live — no rebuild needed."
      }
    },
    "optionalPluginDependencies": {
      "@s2script/zones": "^0.3.0"
    }
  }
}
