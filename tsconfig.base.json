{
  "//": "Authoring config for plugins/examples. Mirrors the `s2script build` typecheck (packages/sdk/src/typecheck/typecheck.ts) so editor IntelliSense matches the build gate exactly. The CLI does NOT read this — it builds its own in-memory program; this file exists only for tsserver/tsc (editor IntelliSense + optional manual `tsc -p`).",
  "compilerOptions": {
    "strict": true,
    "noEmit": true,
    "moduleResolution": "bundler",
    "module": "ESNext",
    "target": "ES2020",
    "lib": ["ES2020"],
    "types": [],
    "baseUrl": "packages",
    "paths": { "@s2script/sdk/*": ["sdk/*.d.ts"], "@s2script/*": ["*/index.d.ts"] },
    "skipLibCheck": true
  }
}
